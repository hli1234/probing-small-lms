<?xml version="1.0" encoding="UTF-8"?>
<rss  xmlns:atom="http://www.w3.org/2005/Atom" 
      xmlns:media="http://search.yahoo.com/mrss/" 
      xmlns:content="http://purl.org/rss/1.0/modules/content/" 
      xmlns:dc="http://purl.org/dc/elements/1.1/" 
      version="2.0">
<channel>
<title>Probing small LMs</title>
<link>https://hli1234.github.io/probing-small-lms/</link>
<atom:link href="https://hli1234.github.io/probing-small-lms/index.xml" rel="self" type="application/rss+xml"/>
<description>Probes, patches, and pictures from the residual stream.</description>
<generator>quarto-1.9.37</generator>
<lastBuildDate>Sun, 10 May 2026 07:00:00 GMT</lastBuildDate>
<item>
  <title>Does GPT-2 know how big a galaxy is?</title>
  <dc:creator>Huangxing Li</dc:creator>
  <link>https://hli1234.github.io/probing-small-lms/posts/physics_axes/</link>
  <description><![CDATA[ 





<section id="the-setup" class="level2">
<h2 class="anchored" data-anchor-id="the-setup">The setup</h2>
<p>In a previous analysis, I demonstrated that the token embedding layer of GPT-2 small represents integer magnitude along a roughly logarithmic axis (<img src="https://latex.codecogs.com/png.latex?R%5E2%20=%200.95"> for <img src="https://latex.codecogs.com/png.latex?%5Clog_2(x)">). This raises a follow-up question: does the same model represent <em>physical</em> magnitudes—such as spatial dimensions, durations, and masses—using a similar logarithmic structure?</p>
<p>The expected outcome is not immediately clear. On one hand, GPT-2’s training data includes texts discussing scales ranging from subatomic particles (<img src="https://latex.codecogs.com/png.latex?10%5E%7B-10%7D"> m) to humans (<img src="https://latex.codecogs.com/png.latex?%5Csim%201"> m) and galaxies (<img src="https://latex.codecogs.com/png.latex?10%5E%7B21%7D"> m). Consolidating these scales into a single “small-to-big” axis would allow the model to efficiently encode approximately 30 orders of magnitude along a single vector direction. On the other hand, terms like <code>electron</code>, <code>kilogram</code>, <code>galaxy</code>, and <code>marathon</code> primarily signify distinct semantic categories (particles, units, celestial structures, and athletic events). Their associated physical dimensions are secondary attributes that the model may not have isolated into a distinct geometric direction at the embedding layer.</p>
<p>To evaluate this, I compiled three lists of physical entities with verified SI values:</p>
<ul>
<li><strong>Length</strong> (19 entities, <img src="https://latex.codecogs.com/png.latex?%5Clog_%7B10%7D"> values spanning <img src="https://latex.codecogs.com/png.latex?-10"> to <img src="https://latex.codecogs.com/png.latex?+21">): <code>atom</code>, <code>virus</code>, <code>cell</code>, <code>meter</code>, <code>inch</code>, <code>mile</code>, <code>marathon</code>, <code>earth</code>, <code>sun</code>, <code>lightyear</code>, <code>parsec</code>, <code>galaxy</code>, etc.</li>
<li><strong>Time</strong> (13 entities, <img src="https://latex.codecogs.com/png.latex?%5Clog_%7B10%7D"> values spanning <img src="https://latex.codecogs.com/png.latex?-9"> to <img src="https://latex.codecogs.com/png.latex?+10.5">): <code>nanosecond</code>, <code>millisecond</code>, <code>second</code>, <code>minute</code>, <code>hour</code>, <code>day</code>, <code>month</code>, <code>year</code>, <code>decade</code>, <code>century</code>, <code>millennium</code>, etc.</li>
<li><strong>Mass</strong> (13 entities, <img src="https://latex.codecogs.com/png.latex?%5Clog_%7B10%7D"> values spanning <img src="https://latex.codecogs.com/png.latex?-30"> to <img src="https://latex.codecogs.com/png.latex?+30">): <code>electron</code>, <code>proton</code>, <code>gram</code>, <code>ounce</code>, <code>pound</code>, <code>kilogram</code>, <code>ton</code>, <code>elephant</code>, <code>whale</code>, <code>earth</code>, <code>sun</code>.</li>
</ul>
<p>For each entity, I extracted the embedding vector for the token <code>" {name}"</code> (using a single leading space) from the layer 0 residual stream. Given the small sample sizes, I applied leave-one-out cross-validation to train a ridge regression probe.</p>
<p>We evaluate two competing hypotheses:</p>
<ul>
<li><strong>H1 (Dimension-specific axes):</strong> Each physical dimension occupies an independent logarithmic axis. Probes trained on one category will fail to generalize to the others.</li>
<li><strong>H2 (Universal magnitude axis):</strong> The model relies on a shared, unit-agnostic “small-to-big” direction, meaning a single probe can predict relative scale across length, time, and mass simultaneously.</li>
</ul>
</section>
<section id="the-result" class="level2">
<h2 class="anchored" data-anchor-id="the-result">The result</h2>
<div id="fig-physics" class="quarto-float quarto-figure quarto-figure-center anchored">
<figure class="quarto-float quarto-float-fig figure">
<div aria-describedby="fig-physics-caption-0ceaefa1-69ba-4598-a22c-09a6ac19f8ca">
<img src="https://hli1234.github.io/probing-small-lms/posts/physics_axes/probe_gpt2_physics.png" class="img-fluid figure-img">
</div>
<figcaption class="quarto-float-caption-bottom quarto-float-caption quarto-float-fig" id="fig-physics-caption-0ceaefa1-69ba-4598-a22c-09a6ac19f8ca">
Figure&nbsp;1: Probing physical-scale axes in GPT-2 small. <strong>Top:</strong> within-category scatter of leave-one-out probe predictions vs true log₁₀(value). Time tracks the diagonal closely (<img src="https://latex.codecogs.com/png.latex?R%5E2%20=%200.81">); length is messy; mass is nearly random. <strong>Bottom left:</strong> cross-category transfer matrix. Diagonal entries are within-category leave-one-out R²; off-diagonal entries are train-on-one, test-on-another. <strong>Bottom middle:</strong> combined probe across all three categories (<img src="https://latex.codecogs.com/png.latex?R%5E2%20=%200.26">). <strong>Bottom right:</strong> summary numbers.
</figcaption>
</figure>
</div>
<p>The primary performance metrics are summarized below:</p>
<table class="caption-top table">
<thead>
<tr class="header">
<th>Category</th>
<th>Within-category log <img src="https://latex.codecogs.com/png.latex?R%5E2"></th>
<th>Within-category linear <img src="https://latex.codecogs.com/png.latex?R%5E2"></th>
</tr>
</thead>
<tbody>
<tr class="odd">
<td><strong>Time</strong></td>
<td><strong>0.81</strong></td>
<td><img src="https://latex.codecogs.com/png.latex?-0.07"></td>
</tr>
<tr class="even">
<td>Length</td>
<td>0.18</td>
<td><img src="https://latex.codecogs.com/png.latex?-0.19"></td>
</tr>
<tr class="odd">
<td>Mass</td>
<td>0.03</td>
<td><img src="https://latex.codecogs.com/png.latex?-0.27"></td>
</tr>
</tbody>
</table>
<p>The probe successfully resolves time on a clear logarithmic scale. Conversely, the representation of length is poorly defined, and mass is statistically indistinguishable from noise.</p>
<p>Cross-category transfer is consistently weak or negative. The only modest exception is the <code>time → mass</code> transfer, which yields an <img src="https://latex.codecogs.com/png.latex?R%5E2%20=%20+0.19">—statistically marginal, but higher than the other cross-category configurations. A unified probe trained across all 45 entities achieves an <img src="https://latex.codecogs.com/png.latex?R%5E2%20=%200.26">, indicating that while some shared variance exists, it is fundamentally weak.</p>
<p>Consequently, neither initial hypothesis is fully supported. There is no clean, universal magnitude axis, yet individual dimension-specific log axes do not consistently emerge either. Instead, the model exhibits an asymmetrical representation: a robust log-time axis, a weak log-length representation, and no discernible log-mass encoding at the embedding level.</p>
</section>
<section id="why-time-works-and-the-others-dont" class="level2">
<h2 class="anchored" data-anchor-id="why-time-works-and-the-others-dont">Why time works and the others don’t</h2>
<p>The performance disparity suggests that the representation of time is driven by linguistic structure rather than physical principles. In English, temporal terms form an explicit, linear hierarchy:</p>
<blockquote class="blockquote">
<p>nanosecond → microsecond → millisecond → second → minute → hour → day → week → month → year → decade → century → millennium</p>
</blockquote>
<p>These tokens are highly interchangeable syntactically and semantically. For example, the phrase <code>"for five {time-word}"</code> remains grammatically and structurally coherent across almost every entry in the sequence. Because these words share nearly all semantic attributes—functioning primarily as temporal units that modify duration—the embedding layer can efficiently compress their variance along a single geometric direction representing scale.</p>
<p>Length and mass terms lack a comparable linear linguistic hierarchy. The length dataset contains a heterogeneous mixture of categories:</p>
<ul>
<li>Units of measurement (<code>millimeter</code>, <code>inch</code>, <code>meter</code>, <code>mile</code>, <code>lightyear</code>, <code>parsec</code>)</li>
<li>Physical objects defined by their scale (<code>atom</code>, <code>virus</code>, <code>cell</code>, <code>galaxy</code>)</li>
<li>Specific macro-entities (<code>earth</code>, <code>sun</code>, <code>moon</code>, <code>marathon</code>)</li>
</ul>
<p>Because these tokens span diverse semantic classes, the embedding for a token like <code>atom</code> primarily encodes its identity as a physical particle rather than its specific spatial dimension (<img src="https://latex.codecogs.com/png.latex?10%5E%7B-10%7D"> m). Similarly, <code>marathon</code> is embedded primarily as a sporting event rather than a representation of a 42 km distance.</p>
<p>The mass category exhibits even greater semantic variance. It groups subatomic particles (<code>electron</code>, <code>proton</code>), standard weights (<code>gram</code>, <code>pound</code>, <code>kilogram</code>), animals (<code>elephant</code>, <code>whale</code>), and astronomical bodies (<code>earth</code>, <code>sun</code>). Because the shared attribute of mass is a tertiary feature of these tokens, the embedding layer organizes them by their primary semantic category rather than extracting their mass magnitude onto a shared axis.</p>
<div class="callout callout-style-default callout-note callout-titled" title="The cleaner test, which I haven't run">
<div class="callout-header d-flex align-content-center">
<div class="callout-icon-container">
<i class="callout-icon"></i>
</div>
<div class="callout-title-container flex-fill">
<span class="screen-reader-only">Note</span>The cleaner test, which I haven’t run
</div>
</div>
<div class="callout-body-container callout-body">
<p>If this linguistic explanation holds, probing the representations <em>with clarifying context</em> should alter the results. Rather than feeding isolated tokens, presenting a template like <code>"The mass of a typical {entity} is"</code> and probing the residual stream at the final token should force the model to isolate the dimension of interest. This contextual constraint should yield cleaner magnitude representations for both length and mass.</p>
</div>
</div>
</section>
<section id="what-it-tells-you-about-llm-physics-intuition" class="level2">
<h2 class="anchored" data-anchor-id="what-it-tells-you-about-llm-physics-intuition">What it tells you about LLM “physics intuition”</h2>
<p>It is sometimes argued that large language models internalize structured, coherent models of physical reality purely from textual associations. For this claim to hold, a model would need to maintain an abstracted representation of scale—an understanding that astronomical objects are large across both spatial and mass dimensions, subatomic entities are uniformly small, and temporal descriptors map to continuous durations.</p>
<p>This experiment serves as a minimal baseline test for that capacity. The results indicate that GPT-2 small constructs a structured representation for <em>time</em>, but fails to do so for <em>length</em> or <em>mass</em> at the foundational layer. A unit-agnostic scale axis is mostly absent (combined <img src="https://latex.codecogs.com/png.latex?R%5E2%20=%200.26">), and cross-category generalizations are largely unsuccessful.</p>
<p>This negative finding requires careful interpretation. The baseline evaluation relies strictly on uncontextualized token embeddings at layer 0 using a limited dataset, meaning it only evaluates whether these axes are explicitly organized at the initial entry point of the network. It is highly probable that:</p>
<ul>
<li>Higher-capacity models (such as Pythia-1.4B or Llama variants) possess sufficient geometric dimensionality to isolate scale metrics from primary semantic categories.</li>
<li>Providing explicit contextual prompts isolates the targeted physical dimension, shifting the residual stream toward scale tracking.</li>
<li>Evaluating layers deeper within the network will show improvement, as intermediate transformer blocks integrate context and map abstract properties onto the token representations.</li>
</ul>
<p>Thus, the baseline conclusion is specific: <strong>at the embedding layer, GPT-2 encodes a structured log-time axis but conflates length and mass magnitudes with primary token semantics</strong>. The model resolves temporal scales more readily than spatial or mass scales because the corresponding English vocabulary is structured as a clear, one-dimensional ladder.</p>
</section>
<section id="a-specific-prediction-and-the-test" class="level2">
<h2 class="anchored" data-anchor-id="a-specific-prediction-and-the-test">A specific prediction — and the test</h2>
<p>The linguistic-ladder hypothesis predicts that contextual prompts will expose the latent scale information for length and mass. By modifying the input from an isolated token to a structured prompt like <code>"The mass of a typical {entity} is about"</code>, the transformer layers should process the underlying physical attribute before reaching the final token (<code>"about"</code>). This structural constraint should surface the hidden magnitude information along a linear direction accessible to linear probes.</p>
<p>To verify this, I re-ran the evaluations using contextualized inputs. The prompt template was set to <code>"The {size|duration|mass} of a typical {entity} is about"</code>, and the residual stream was probed at the final token position across all network layers.</p>
<div class="callout callout-style-default callout-important callout-titled" title="Prediction confirmed">
<div class="callout-header d-flex align-content-center">
<div class="callout-icon-container">
<i class="callout-icon"></i>
</div>
<div class="callout-title-container flex-fill">
<span class="screen-reader-only">Important</span>Prediction confirmed
</div>
</div>
<div class="callout-body-container callout-body">
<p>Introducing contextual prompts causes the peak layer performance for mass to rise from <strong>0.45 to 0.84</strong> <img src="https://latex.codecogs.com/png.latex?R%5E2">. Time increases from 0.88 to 0.95, and length improves from 0.30 to 0.48. This confirms that the underlying scale data is accessible within the model; the uncontextualized embedding token simply stores it orthogonal to a standard linear probe direction.</p>
</div>
</div>
<div id="fig-physics-context" class="quarto-float quarto-figure quarto-figure-center anchored">
<figure class="quarto-float quarto-float-fig figure">
<div aria-describedby="fig-physics-context-caption-0ceaefa1-69ba-4598-a22c-09a6ac19f8ca">
<img src="https://hli1234.github.io/probing-small-lms/posts/physics_axes/probe_gpt2_physics_context.png" class="img-fluid figure-img">
</div>
<figcaption class="quarto-float-caption-bottom quarto-float-caption quarto-float-fig" id="fig-physics-context-caption-0ceaefa1-69ba-4598-a22c-09a6ac19f8ca">
Figure&nbsp;2: Layer-by-layer leave-one-out R² for <img src="https://latex.codecogs.com/png.latex?%5Clog_%7B10%7D(%5Ctext%7BSI%20value%7D)">, in two conditions: bare entity token (blue) versus contextualised prompt at the last token (red). For all three categories, contextualised probing reaches a higher peak R². The largest jump is for mass (0.45 → 0.84), confirming that the bare token was hiding magnitude behind semantic category. The layer profile is also informative: bare-token time peaks at layer 2 (embedding-level info), while contextualised probes peak near the final layer (context integration).
</figcaption>
</figure>
</div>
<p>The layer-by-layer performance profiles reveal different processing dynamics:</p>
<ul>
<li><strong>Uncontextualized time</strong> peaks early at <em>layer 2</em> and steadily decays in subsequent layers. This indicates that temporal scale information is encoded directly within the initial token representations and becomes diffused during deeper network processing. The temporal hierarchy is natively embedded in the token relationships.</li>
<li><strong>Contextualized attributes (time, length, and mass)</strong> uniformly peak at or near the <em>final network layer</em>. This confirms that the scale representations are dynamically computed and assembled across successive transformer blocks rather than being retrieved from the embedding layer.</li>
<li><strong>Uncontextualized mass</strong> rises from an <img src="https://latex.codecogs.com/png.latex?R%5E2%20=%200.03"> at layer 0 to a peak of <img src="https://latex.codecogs.com/png.latex?0.45"> at layer 9 before declining. Even without explicit prompt steering, the intermediate layers partially isolate the mass attribute from raw token semantics, though less effectively than the context-steered model.</li>
</ul>
<p>In summary, GPT-2 preserves physical scale information across all three tested dimensions, but only encodes it natively at the embedding layer for time. Resolving length and mass scales requires contextual processing across the network blocks. The structural difference is real, but it reflects how the information is routed rather than its complete absence. Linear probing on bare tokens measures embedding organization, not the limits of the model’s downstream capacity.</p>
</section>
<section id="whats-next" class="level2">
<h2 class="anchored" data-anchor-id="whats-next">What’s next</h2>
<p>Following the verification of the context-driven retrieval, several lines of inquiry open up:</p>
<p><strong>Dimensional analysis:</strong> Does GPT-2 maintain a structured representation of units? If prompted with <code>"velocity has units of"</code>, can its internal representations distinguish dimensionally valid expressions like <code>m/s</code> from dimensionally wrong ones like <code>s/m</code>? Probing whether the residual stream can linearize dimensional formulas would test its structural grasp of unit equations.</p>
<p><strong>Order-of-magnitude estimation:</strong> Evaluating standard Fermi approximation problems (e.g., estimating regional labor statistics) can test whether the model executes multi-step scale arithmetic. If prior findings regarding log-axis shortcuts hold true, the network should resolve these calculations via vector addition along its internal logarithmic scales.</p>
<p><strong>Cross-modal scale alignment:</strong> Evaluating multimodal models (such as CLIP variants) could determine if a shared “scale vector” aligns corresponding textual and visual tokens (e.g., mapping an image of a cellular structure to an image of a planetary system). A model exposed to diverse multimodal inputs may learn a clean scale axis from visual framing cues like magnification or perspective.</p>
<p><strong>Direct isolation of the linguistic-ladder effect:</strong> This hypothesis can be isolated by probing abstract concept hierarchies that form explicit verbal scales independently of physical units. For instance, testing temperature sequences (<code>cold</code>, <code>cool</code>, <code>warm</code>, <code>hot</code>, <code>boiling</code>) or velocity descriptors (<code>slow</code>, <code>walking</code>, <code>running</code>, <code>flying</code>) should yield clean linear probe performance at layer 0, whereas semantic categories that lack a linear hierarchy should uniformly fail.</p>
</section>
<section id="code" class="level2">
<h2 class="anchored" data-anchor-id="code">Code</h2>
<ul>
<li><code>probe_gpt2_physics.py</code> — Script for the isolated bare-token evaluations.</li>
<li><code>probe_gpt2_physics_context.py</code> — Script for the contextualized evaluations and layer-by-layer tracking.</li>
</ul>
<p>Both files execute in under one minute on standard CPU architectures.</p>
<hr>
<p><em>Characterizing a model’s physical comprehension requires separating representation from accessibility. GPT-2 small retains robust scale representations across all three dimensions when evaluated using contextual prompts (<img src="https://latex.codecogs.com/png.latex?R%5E2%20%5Cin%20%5B0.48,%200.95%5D">). However, only temporal terms store this magnitude information natively within the embedding layer. Spatial and mass values are latent and must be actively extracted by the network layers through contextual processing.</em></p>


<!-- -->

</section>

 ]]></description>
  <category>interpretability</category>
  <category>physics</category>
  <category>language models</category>
  <guid>https://hli1234.github.io/probing-small-lms/posts/physics_axes/</guid>
  <pubDate>Sun, 10 May 2026 07:00:00 GMT</pubDate>
</item>
<item>
  <title>Looking for the log-multiplication trick inside a small language model</title>
  <dc:creator>Huangxing Li</dc:creator>
  <link>https://hli1234.github.io/probing-small-lms/posts/log_shortcut_findings/</link>
  <description><![CDATA[ 





<section id="the-question" class="level2">
<h2 class="anchored" data-anchor-id="the-question">The question</h2>
<p>For three centuries prior to the advent of digital calculators, scientific multiplication relied heavily on slide rules. The physical slide rule maps arithmetic computation onto a foundational geometric identity:</p>
<p><span id="eq-shortcut"><img src="https://latex.codecogs.com/png.latex?%0Aa%20%5Ccdot%20b%20=%202%5E%7B%5Clog_2%20a%20+%20%5Clog_2%20b%7D%0A%5Ctag%7B1%7D"></span></p>
<p>By computing spatial additions across logarithmic spaces, this layout trades complex multi-digit multiplication for three simple lookups and a single addition. Distances on a slide rule are spaced by <img src="https://latex.codecogs.com/png.latex?%5Clog_2(x)"> rather than linear magnitude, meaning that the physical transposition of parts mechanically performs exponential scaling.</p>
<p>Whether an artificial neural network scales numerical features along an identical internal geometry remains an open question. When exposed to text containing frequent numerical patterns, does a standard transformer develop an internal representation analogous to a slide rule?</p>
<p>While a comprehensive mechanistic investigation would involve training custom architectures from scratch under explicit capacity constraints, this evaluation targets a more direct question: <strong>is a log-multiplication shortcut already present within existing small pre-trained language models?</strong></p>
<p>To trace this behavior, I evaluated GPT-2 small (124M parameters), Pythia-70M, and Pythia-160M. The evaluation relies on two primary interpretability paradigms: <strong>linear probing</strong> to read localized semantic features directly from the residual stream, and <strong>activation patching</strong> to test the downstream causal impact of surgically modifying vector coordinates along targeted directions.</p>
<div class="callout callout-style-default callout-note callout-titled" title="Summary in one figure">
<div class="callout-header d-flex align-content-center">
<div class="callout-icon-container">
<i class="callout-icon"></i>
</div>
<div class="callout-title-container flex-fill">
<span class="screen-reader-only">Note</span>Summary in one figure
</div>
</div>
<div class="callout-body-container callout-body">
<div id="fig-summary" class="quarto-float quarto-figure quarto-figure-center anchored">
<figure class="quarto-float quarto-float-fig figure">
<div aria-describedby="fig-summary-caption-0ceaefa1-69ba-4598-a22c-09a6ac19f8ca">
<img src="https://hli1234.github.io/probing-small-lms/posts/log_shortcut_findings/summary_figure.png" class="img-fluid figure-img">
</div>
<figcaption class="quarto-float-caption-bottom quarto-float-caption quarto-float-fig" id="fig-summary-caption-0ceaefa1-69ba-4598-a22c-09a6ac19f8ca">
Figure&nbsp;1: Six panels covering three models (GPT-2 small / Pythia-70M / Pythia-160M). <strong>A:</strong> Embedding log-axis decodability in all three. Log dominates only under weight tying; untied Pythia models prefer linear by a hair. <strong>B:</strong> GPT-2 <em>does</em> compute <img src="https://latex.codecogs.com/png.latex?%5Clog_2(a%20%5Ccdot%20b)"> at the equals-sign at <img src="https://latex.codecogs.com/png.latex?R%5E2%20=%200.989"> — but on a different axis than the one used to read the operands. <strong>C:</strong> New result. Patching along that fresh answer-side direction moves the LM head’s argmax with slope only <img src="https://latex.codecogs.com/png.latex?0.07"> — the probe is correlational, not causal. <strong>D:</strong> LM-head log-axis fraction collapses to baseline once weights are untied, so GPT-2’s “28× baseline” was mostly architectural. <strong>E:</strong> Near-miss slope <img src="https://latex.codecogs.com/png.latex?%5Cbeta"> is invariant at ~0.22 across all three models, even as panels A and D vary by orders of magnitude. <strong>F:</strong> synthesis.
</figcaption>
</figure>
</div>
</div>
</div>
</section>
<section id="a-simple-model-first-when-should-a-learner-discover-the-shortcut" class="level2">
<h2 class="anchored" data-anchor-id="a-simple-model-first-when-should-a-learner-discover-the-shortcut">A simple model first: when <em>should</em> a learner discover the shortcut?</h2>
<p>To understand the optimization trade-offs, I first formulated a toy mathematical routing model to quantify cost pressures. Consider two independent computational pathways to solve for <img src="https://latex.codecogs.com/png.latex?z%20=%20a%20%5Ccdot%20b">:</p>
<ul>
<li><strong>Direct multiplication:</strong> This path scales with numerical complexity, carrying an algorithmic cost of <img src="https://latex.codecogs.com/png.latex?c_%7B%5Cmathrm%7Bmul%7D%7D(a,%20b)%20=%20%5Clog_2(a)%20%5Ccdot%20%5Clog_2(b)">. Simpler calculations (e.g., small single-digit values) require minimal bit manipulation, whereas macro-integers scale heavily.</li>
<li><strong>Log–sum–exp shortcut:</strong> This path operates via fixed lookups with a static complexity baseline of <img src="https://latex.codecogs.com/png.latex?c_%7B%5Cmathrm%7Bshort%7D%7D%20=%206">, remaining uniform regardless of operand magnitude.</li>
</ul>
<p>We can define a learnable gating parameter <img src="https://latex.codecogs.com/png.latex?p%20%5Cin%20%5B0,%201%5D"> that observes <img src="https://latex.codecogs.com/png.latex?(%5Clog_2%20a,%20%5Clog_2%20b)"> to optimize path routing. Given a specific multiplication complexity penalty <img src="https://latex.codecogs.com/png.latex?%5Clambda">, the expected total cost is defined as:</p>
<p><span id="eq-cost"><img src="https://latex.codecogs.com/png.latex?%0AC(p,%20a,%20b,%20%5Clambda)%20=%20(1-p)%5Ccdot%20%5Clambda%20%5Ccdot%20c_%7B%5Cmathrm%7Bmul%7D%7D(a,%20b)%20+%20p%20%5Ccdot%20c_%7B%5Cmathrm%7Bshort%7D%7D.%0A%5Ctag%7B2%7D"></span></p>
<p>Sweeping across values of <img src="https://latex.codecogs.com/png.latex?%5Clambda"> traces a clear optimization boundary. Complex operand pairs switch to the fixed-cost shortcut early, whereas simple pairs resist routing changes. For trivial values involving <img src="https://latex.codecogs.com/png.latex?1">, direct calculation remains free because <img src="https://latex.codecogs.com/png.latex?%5Clog_2(1)%20=%200">, completely bypassing the need for a shortcut.</p>
<div id="fig-toy-routing" class="quarto-float quarto-figure quarto-figure-center anchored">
<figure class="quarto-float quarto-float-fig figure">
<div aria-describedby="fig-toy-routing-caption-0ceaefa1-69ba-4598-a22c-09a6ac19f8ca">
<img src="https://hli1234.github.io/probing-small-lms/posts/log_shortcut_findings/reasoning_path_vs_penalty.png" class="img-fluid figure-img">
</div>
<figcaption class="quarto-float-caption-bottom quarto-float-caption quarto-float-fig" id="fig-toy-routing-caption-0ceaefa1-69ba-4598-a22c-09a6ac19f8ca">
Figure&nbsp;2: Toy soft-routing model under varying multiplication-penalty weight <img src="https://latex.codecogs.com/png.latex?%5Clambda">. <strong>Top:</strong> average shortcut probability across <img src="https://latex.codecogs.com/png.latex?(a,%20b)%20%5Cin%20%5B1,%2064%5D%5E2">. The trained gate matches the analytical softmin to three decimal places. <strong>Bottom:</strong> per-pair gate at five snapshot <img src="https://latex.codecogs.com/png.latex?%5Clambda"> values; the decision boundary sweeps from upper-right (hard pairs) to lower-left (easy pairs) as the penalty grows.
</figcaption>
</figure>
</div>
<p>Rather than acting as an empirical result, this model serves as a quantitative baseline. It provides a geometric hypothesis to compare against when testing whether real, text-trained models show any underlying trace of this efficiency-driven routing.</p>
</section>
<section id="how-gpt-2-represents-numbers" class="level2">
<h2 class="anchored" data-anchor-id="how-gpt-2-represents-numbers">How GPT-2 represents numbers</h2>
<p>To investigate how a model processes integer values internally, we can query the residual stream at a specific numerical token position. If a linear regression probe can accurately recover <img src="https://latex.codecogs.com/png.latex?%5Clog_2(x)"> from these hidden states, it indicates that the model isolates a distinct logarithmic vector direction within that layer.</p>
<p>I passed integer sequences from 2 to 1023 into GPT-2 small as isolated space-prefixed tokens (<code>" 5"</code>, <code>" 17"</code>, <code>" 1024"</code>). At each transformer block, the hidden states were extracted to train a ridge regression probe targeted at tracking <img src="https://latex.codecogs.com/png.latex?%5Clog_2(x)"> along with several operational controls.</p>
<p><strong>At the raw token embedding layer (layer 0)—prior to any attention or MLP processing—<img src="https://latex.codecogs.com/png.latex?%5Clog_2(x)"> is cleanly decodable with a cross-validated <img src="https://latex.codecogs.com/png.latex?R%5E2%20=%200.95">.</strong> Direct linear scaling (<img src="https://latex.codecogs.com/png.latex?x">) is significantly less distinct, yielding an <img src="https://latex.codecogs.com/png.latex?R%5E2%20=%200.77">, while randomized control labels drop completely to <img src="https://latex.codecogs.com/png.latex?R%5E2%20%5Capprox%200">. This confirms a clear embedding preference for logarithmic geometry.</p>
<p>To verify whether this axis represents a precise logarithmic scale rather than a generic monotonic trend, I evaluated a parametric family of power-law transformations (<img src="https://latex.codecogs.com/png.latex?x%5E%5Calpha">). Probing performance peaks narrowly at <img src="https://latex.codecogs.com/png.latex?%5Calpha%20=%200">, which corresponds mathematically to the logarithmic limit. Had the embedding prioritized linear magnitude, performance would have instead centered at <img src="https://latex.codecogs.com/png.latex?%5Calpha%20=%201">.</p>
<p>The embedding layer also natively encodes several non-linear attributes. Vector directions for numerical parity achieve a decodability of <img src="https://latex.codecogs.com/png.latex?R%5E2%20=%200.94">, and the final digit (<img src="https://latex.codecogs.com/png.latex?x%20%5Cbmod%2010">) reaches an <img src="https://latex.codecogs.com/png.latex?R%5E2%20=%200.92">. GPT-2 uncovers surface-level linguistic associations (e.g., clustering terms ending in <code>5</code>), yet remains entirely insensitive to deeper number-theoretic features like primality or divisibility by <img src="https://latex.codecogs.com/png.latex?7">, which fail to emerge above chance levels.</p>
<div id="fig-controls" class="quarto-float quarto-figure quarto-figure-center anchored">
<figure class="quarto-float quarto-float-fig figure">
<div aria-describedby="fig-controls-caption-0ceaefa1-69ba-4598-a22c-09a6ac19f8ca">
<img src="https://hli1234.github.io/probing-small-lms/posts/log_shortcut_findings/probe_gpt2_controls.png" class="img-fluid figure-img">
</div>
<figcaption class="quarto-float-caption-bottom quarto-float-caption quarto-float-fig" id="fig-controls-caption-0ceaefa1-69ba-4598-a22c-09a6ac19f8ca">
Figure&nbsp;3: Linear-probe <img src="https://latex.codecogs.com/png.latex?R%5E2"> at each layer for monotone targets, non-monotone targets, and a noise baseline. The log axis, parity, and last digit are all decodable at the embedding (<img src="https://latex.codecogs.com/png.latex?R%5E2%20%3E%200.9">). Number-theoretic properties (mod 7, primality) and the shuffle control are at chance.
</figcaption>
</figure>
</div>
<section id="does-this-generalise-across-categories-of-numbers" class="level3">
<h3 class="anchored" data-anchor-id="does-this-generalise-across-categories-of-numbers">Does this generalise across categories of numbers?</h3>
<p>We can test whether this logarithmic vector space acts as a universal magnitude axis that extends uniformly across diverse numeric formats, such as calendar years (<code>" 1990"</code>), round numbers (<code>" 1000"</code>), or spelled-out words (<code>" million"</code>).</p>
<p>The empirical data rejects this hypothesis. Applying the probe trained strictly on standard integers across these secondary groups yields highly degraded transfer metrics:</p>
<ul>
<li><strong>Calendar Years</strong> (152 single-token years between 1500 and 2099): <img src="https://latex.codecogs.com/png.latex?R%5E2%20=%20-2274">. The probe yields near-constant values, completely failing to separate the relative scale of distinct years.</li>
<li><strong>Round Multiples</strong> (33 single-token multiples of 100 up to 100,000): <img src="https://latex.codecogs.com/png.latex?R%5E2%20=%200.22">, showing highly fragmented tracking.</li>
<li><strong>Spelled Numbers</strong> (24 English number words): <img src="https://latex.codecogs.com/png.latex?R%5E2%20=%200.14">, demonstrating no structural alignment.</li>
</ul>
<div id="fig-shared-axis" class="quarto-float quarto-figure quarto-figure-center anchored">
<figure class="quarto-float quarto-float-fig figure">
<div aria-describedby="fig-shared-axis-caption-0ceaefa1-69ba-4598-a22c-09a6ac19f8ca">
<img src="https://hli1234.github.io/probing-small-lms/posts/log_shortcut_findings/probe_gpt2_shared_axis.png" class="img-fluid figure-img">
</div>
<figcaption class="quarto-float-caption-bottom quarto-float-caption quarto-float-fig" id="fig-shared-axis-caption-0ceaefa1-69ba-4598-a22c-09a6ac19f8ca">
Figure&nbsp;4: The integer-trained log probe applied to other numeric categories. Integers (training) sit cleanly on <img src="https://latex.codecogs.com/png.latex?y%20=%20x">. Years, round numbers, and number-words sit far off the line. Each numeric category lives on its own axis in embedding space; there is no universal magnitude direction.
</figcaption>
</figure>
</div>
<p>This demonstrates that numerical encodings are highly dependent on their specific context and textual role rather than their raw scalar values. The token (<code>" 1990"</code>) is processed primarily as a temporal marker rather than its underlying physical magnitude. Each distinct numerical category occupies an isolated coordinate space within the embedding.</p>
</section>
</section>
<section id="does-the-log-axis-actually-do-anything" class="level2">
<h2 class="anchored" data-anchor-id="does-the-log-axis-actually-do-anything">Does the log axis actually do anything?</h2>
<p>Because linear decodability establishes correlation rather than causation, I applied activation patching at the operand position to verify whether the model actively routes information along this logarithmic axis during multiplication tasks.</p>
<p>Consider a baseline prompt like <code>" 5 * 9 ="</code>. At the vector coordinates representing <code>" 5"</code>, the embedding’s projection along the discovered probe direction <img src="https://latex.codecogs.com/png.latex?w"> scales cleanly with <img src="https://latex.codecogs.com/png.latex?%5Clog_2(5)%20%5Capprox%202.3">. By applying a rank-1 vector shift along <img src="https://latex.codecogs.com/png.latex?w">, I adjusted this projection to map exactly to <img src="https://latex.codecogs.com/png.latex?%5Clog_2(2)%20=%201">, leaving orthogonal features like parity and last-digit identities unchanged. The network was then allowed to complete its forward pass across all 12 layers.</p>
<p>For the prompt <code>" 5 * 9 ="</code>, shifting the operand vector from <img src="https://latex.codecogs.com/png.latex?%5Clog_2(5)%20%5Cto%20%5Clog_2(2)"> causes the output logit for <code>" 18"</code> (<img src="https://latex.codecogs.com/png.latex?2%20%5Ccdot%209">) to rise cleanly above the true answer of <code>" 45"</code> (<img src="https://latex.codecogs.com/png.latex?5%20%5Ccdot%209">). Artificially patching the coordinate toward <img src="https://latex.codecogs.com/png.latex?4"> smoothly shifts the top output token to <code>" 36"</code>. Conversely, applying equivalent vector interventions along random control paths yields no measurable logit shifts, proving that the causal effect is structurally isolated along the probed direction.</p>
<p>The intervention reveals a notable asymmetry: downward patches (scaling to smaller values) shift output probabilities cleanly, whereas upward patches face significant resistance. This indicates that the language model head maintains a strong frequency prior that naturally penalizes large or rare numbers, readily accepting downward modifications while resisting shifts toward less common numerical outputs.</p>
<div id="fig-patching" class="quarto-float quarto-figure quarto-figure-center anchored">
<figure class="quarto-float quarto-float-fig figure">
<div aria-describedby="fig-patching-caption-0ceaefa1-69ba-4598-a22c-09a6ac19f8ca">
<img src="https://hli1234.github.io/probing-small-lms/posts/log_shortcut_findings/probe_gpt2_patching.png" class="img-fluid figure-img">
</div>
<figcaption class="quarto-float-caption-bottom quarto-float-caption quarto-float-fig" id="fig-patching-caption-0ceaefa1-69ba-4598-a22c-09a6ac19f8ca">
Figure&nbsp;5: Causal patching at the operand position. <strong>Left:</strong> change in relative logit, <img src="https://latex.codecogs.com/png.latex?%5Cmathrm%7Blogit%7D(a_%7B%5Cmathrm%7Btarget%7D%7D%20%5Ccdot%20b)%20-%20%5Cmathrm%7Blogit%7D(a%20%5Ccdot%20b)">, as we patch the log direction at the <code>" a"</code> position. Solid lines are the log direction; dotted lines are random directions of the same norm. <strong>Right:</strong> heatmap of relative logits for <code>" 5 * 9 ="</code>. The yellow most-favoured-product stripe follows the perfect-log-shortcut line <img src="https://latex.codecogs.com/png.latex?p%20=%20a_%7B%5Cmathrm%7Btarget%7D%7D%20%5Ccdot%20b"> for downward patches, then sticks at 45 once we patch toward larger numbers.
</figcaption>
</figure>
</div>
</section>
<section id="does-the-model-actually-compute-logab" class="level2">
<h2 class="anchored" data-anchor-id="does-the-model-actually-compute-logab">Does the model actually compute log(a·b)?</h2>
<p>If a transformer implements an internal slide-rule mechanism, it must add the operand logarithms together and express the total product <img src="https://latex.codecogs.com/png.latex?%5Clog_2(a%20%5Ccdot%20b)"> within the residual stream at the equals-sign token (<code>"="</code>) right before the final prediction head.</p>
<p>To test this, I evaluated whether the operand-side probe vector <img src="https://latex.codecogs.com/png.latex?w"> could read the combined product scale at the final token position. For <code>" 5 * 9 ="</code>, the probe reads <strong>10.76</strong> (instead of <img src="https://latex.codecogs.com/png.latex?%5Clog_2(45)%20%5Capprox%205.49">). For <code>" 3 * 7 ="</code>, it registers <strong>11.17</strong> (instead of <img src="https://latex.codecogs.com/png.latex?%5Clog_2(21)%20%5Capprox%204.39">). Evaluating across multiple combinations reveals a flat response curve, yielding an <img src="https://latex.codecogs.com/png.latex?R%5E2%20=%200.011"> between the probe’s readouts and the true product value.</p>
<p>This initial result suggests that the complete slide-rule circuit is absent; while the operands are tracked logarithmically at the input, the values do not appear to combine along this vector direction at the output stage.</p>
<p>However, a transformer is not architecturally constrained to reuse the same vector space across different token positions. The model can map intermediate transformations onto an entirely fresh coordinate axis. To test this, I trained a separate ridge probe directly on the hidden states of the <code>"="</code> token across hundreds of random calculation pairs.</p>
<div class="callout callout-style-default callout-important callout-titled" title="The key result">
<div class="callout-header d-flex align-content-center">
<div class="callout-icon-container">
<i class="callout-icon"></i>
</div>
<div class="callout-title-container flex-fill">
<span class="screen-reader-only">Important</span>The key result
</div>
</div>
<div class="callout-body-container callout-body">
<p>This output-side probe successfully recovers the true product magnitude <img src="https://latex.codecogs.com/png.latex?%5Clog_2(a%20%5Ccdot%20b)"> with a cross-validated <strong><img src="https://latex.codecogs.com/png.latex?R%5E2%20=%200.989"></strong>. The logarithmic representation of the answer is fully constructed. The network translates the individual operand components from their input axes, sums them, and maps the total product onto a completely distinct vector direction at the terminal token position.</p>
</div>
</div>
<div id="fig-pipeline" class="quarto-float quarto-figure quarto-figure-center anchored">
<figure class="quarto-float quarto-float-fig figure">
<div aria-describedby="fig-pipeline-caption-0ceaefa1-69ba-4598-a22c-09a6ac19f8ca">
<img src="https://hli1234.github.io/probing-small-lms/posts/log_shortcut_findings/probe_gpt2_pipeline.png" class="img-fluid figure-img">
</div>
<figcaption class="quarto-float-caption-bottom quarto-float-caption quarto-float-fig" id="fig-pipeline-caption-0ceaefa1-69ba-4598-a22c-09a6ac19f8ca">
Figure&nbsp;6: The end-to-end pipeline. <strong>Left:</strong> patching the log component at the <code>=</code> position of real prompts; argmax over numeric tokens is stuck at <code>" 1"</code> regardless of <img src="https://latex.codecogs.com/png.latex?v_%7B%5Cmathrm%7Btarget%7D%7D"> . <strong>Middle:</strong> the operand-side probe’s readout at <code>=</code> is roughly constant (<img src="https://latex.codecogs.com/png.latex?%5Csim%2010.8">) across all prompts, uncorrelated with <img src="https://latex.codecogs.com/png.latex?%5Clog_2(a%20%5Ccdot%20b)"> (<img src="https://latex.codecogs.com/png.latex?R%5E2%20=%200.011">). <strong>Right:</strong> a fresh probe trained directly on residuals at <code>=</code> predicts <img src="https://latex.codecogs.com/png.latex?%5Clog_2(a%20%5Ccdot%20b)"> at <img src="https://latex.codecogs.com/png.latex?R%5E2%20=%200.989"> on held-out pairs.
</figcaption>
</figure>
</div>
<section id="a-correction-the-answer-side-direction-is-correlational-not-causal" class="level3">
<h3 class="anchored" data-anchor-id="a-correction-the-answer-side-direction-is-correlational-not-causal">A correction: the answer-side direction is correlational, not causal</h3>
<p>While achieving a decoding metric of <img src="https://latex.codecogs.com/png.latex?R%5E2%20=%200.989"> is notable, it is critical to verify whether this output direction is causally driving token generation or if it represents a passive downstream correlation—a phenomenon known as a probing illusion.</p>
<p>To establish causal validity, I performed a directional patching sweep along this output vector space (<img src="https://latex.codecogs.com/png.latex?w_%7B%5Cmathrm%7Bans%7D%7D">) across various target scales (<img src="https://latex.codecogs.com/png.latex?v_%7B%5Cmathrm%7Btarget%7D%7D">). If the final prediction layer directly relies on this direction to select tokens, the generated values should scale linearly with the intervention, tracing a slope of <img src="https://latex.codecogs.com/png.latex?1"> on a log-log plot.</p>
<p>The experimental intervention reveals a causal slope of only <strong>0.072</strong>. For the vast majority of mathematical prompts, modifying the internal states along this vector direction across an 11-unit log range fails to alter the output token. While <img src="https://latex.codecogs.com/png.latex?%5Clog_2(a%20%5Ccdot%20b)"> is highly decodable from this space, the prediction head does not rely on this specific vector coordinate to select numbers.</p>
<div class="callout callout-style-default callout-warning callout-titled" title="Honest revision">
<div class="callout-header d-flex align-content-center">
<div class="callout-icon-container">
<i class="callout-icon"></i>
</div>
<div class="callout-title-container flex-fill">
<span class="screen-reader-only">Warning</span>Honest revision
</div>
</div>
<div class="callout-body-container callout-body">
<p>A strictly correlational analysis of the <img src="https://latex.codecogs.com/png.latex?R%5E2%20=%200.989"> metric would conclude that the complete log-arithmetic pathway is fully integrated. However, the causal intervention proves that the language model head bypasses this specific axis during final token selection. The model isolates a logarithmic tracking space for the combined product, but maps its final behavioral selections along independent, unaligned coordinate directions.</p>
</div>
</div>
<div id="fig-answer-side-causal" class="quarto-float quarto-figure quarto-figure-center anchored">
<figure class="quarto-float quarto-float-fig figure">
<div aria-describedby="fig-answer-side-causal-caption-0ceaefa1-69ba-4598-a22c-09a6ac19f8ca">
<img src="https://hli1234.github.io/probing-small-lms/posts/log_shortcut_findings/probe_gpt2_answer_side_causal.png" class="img-fluid figure-img">
</div>
<figcaption class="quarto-float-caption-bottom quarto-float-caption quarto-float-fig" id="fig-answer-side-causal-caption-0ceaefa1-69ba-4598-a22c-09a6ac19f8ca">
Figure&nbsp;7: Patching along the fitted answer-side direction <img src="https://latex.codecogs.com/png.latex?w_%7B%5Cmathrm%7Bans%7D%7D"> at the <code>=</code> position. <strong>Left:</strong> for each of eight multiplication prompts, <img src="https://latex.codecogs.com/png.latex?%5Clog_2"> of the LM head’s argmax-numeric vs the injected <img src="https://latex.codecogs.com/png.latex?v_%7B%5Cmathrm%7Btarget%7D%7D">. Compared to the dashed reference <img src="https://latex.codecogs.com/png.latex?y%20=%20v_%7B%5Cmathrm%7Btarget%7D%7D">, the curves are essentially flat — the LM head doesn’t read this direction. <strong>Right:</strong> rank of the closest single-token integer to <img src="https://latex.codecogs.com/png.latex?2%5E%7Bv_%7B%5Cmathrm%7Btarget%7D%7D%7D"> among all numeric tokens. The target never ends up at rank 0; it bottoms around 100-300 out of 638 numeric tokens.
</figcaption>
</figure>
</div>
</section>
</section>
<section id="two-more-checks-was-the-other-finding-real-or-an-artifact" class="level2">
<h2 class="anchored" data-anchor-id="two-more-checks-was-the-other-finding-real-or-an-artifact">Two more checks: was the other finding real or an artifact?</h2>
<p>Given these conflicting causal insights, I implemented two deeper quantitative checks to calibrate the behavioral boundaries of this system.</p>
<section id="check-1-does-the-lm-head-act-on-the-log-axis" class="level3">
<h3 class="anchored" data-anchor-id="check-1-does-the-lm-head-act-on-the-log-axis">Check 1 — does the LM head act on the log axis?</h3>
<p>The rows of the final logit projection matrix determine how the internal vectors map to token output space. If the logarithmic axis directly steers the output distribution, these row projections should hold a prominent geometric presence within the weight matrix.</p>
<p>In GPT-2 small, the discovered logarithmic vector accounts for 3.6% of the average squared norm across numerical token projection rows, standing significantly above a uniform isotropic baseline (<img src="https://latex.codecogs.com/png.latex?1/d%20=%200.13%5C%25">).</p>
<p>However, GPT-2 small utilizes <strong>tied weights</strong>, meaning the output projection matrix is identical to the input embedding matrix. The geometric alignment is an architectural constraint rather than an emerged circuit. To isolate this, I repeated the projection analysis on <strong>Pythia-70M</strong> and <strong>Pythia-160M</strong>, which feature untied matrices:</p>
<table class="caption-top table">
<colgroup>
<col style="width: 20%">
<col style="width: 20%">
<col style="width: 20%">
<col style="width: 20%">
<col style="width: 20%">
</colgroup>
<thead>
<tr class="header">
<th>Model</th>
<th>Hidden Dimension <img src="https://latex.codecogs.com/png.latex?d"></th>
<th>Weight Tying</th>
<th>Log-Axis Projection Share</th>
<th>Relative to Baseline (<img src="https://latex.codecogs.com/png.latex?1/d">)</th>
</tr>
</thead>
<tbody>
<tr class="odd">
<td>GPT-2 small (124M)</td>
<td>768</td>
<td>Tied</td>
<td>3.6%</td>
<td><strong>28×</strong></td>
</tr>
<tr class="even">
<td>Pythia-70M</td>
<td>512</td>
<td>Untied</td>
<td>0.19%</td>
<td><strong>1.0×</strong></td>
</tr>
<tr class="odd">
<td>Pythia-160M</td>
<td>768</td>
<td>Untied</td>
<td>0.01%</td>
<td><strong>0.1×</strong></td>
</tr>
</tbody>
</table>
<p>When weight tying is eliminated, the projection rows do not align with the integer embedding’s log axis. While the underlying logarithmic representation remains highly decodable across all three model embeddings (see panel A of Figure&nbsp;1), the prediction layer does not automatically preserve this alignment in untied architectures. The apparent structural integration observed in GPT-2 is primarily a byproduct of its shared weight configuration.</p>
<div id="fig-lmhead" class="quarto-float quarto-figure quarto-figure-center anchored">
<figure class="quarto-float quarto-float-fig figure">
<div aria-describedby="fig-lmhead-caption-0ceaefa1-69ba-4598-a22c-09a6ac19f8ca">
<img src="https://hli1234.github.io/probing-small-lms/posts/log_shortcut_findings/probe_gpt2_lmhead.png" class="img-fluid figure-img">
</div>
<figcaption class="quarto-float-caption-bottom quarto-float-caption quarto-float-fig" id="fig-lmhead-caption-0ceaefa1-69ba-4598-a22c-09a6ac19f8ca">
Figure&nbsp;8: LM-head log-axis decomposition for GPT-2 small. The 3.6% number averaged over numeric tokens is real, but the comparison with Pythia (see panel C of Figure&nbsp;1) shows most of that comes from weight tying.
</figcaption>
</figure>
</div>
</section>
<section id="check-2-does-the-log-axis-pull-on-the-actual-output-distribution" class="level3">
<h3 class="anchored" data-anchor-id="check-2-does-the-log-axis-pull-on-the-actual-output-distribution">Check 2 — does the log axis pull on the actual output distribution?</h3>
<p>To measure pure behavioral influence across 200 random multiplication prompts <img src="https://latex.codecogs.com/png.latex?(a,%20b)%20%5Cin%20%5B2,%2050)%5E2">, I isolated the generated probability distributions across numerical tokens and fitted them to a standard log-Gaussian model:</p>
<p><span id="eq-gauss"><img src="https://latex.codecogs.com/png.latex?%0AP(%5Ctext%7Bpredict%20%7D%20n%20%5Cmid%20a,%20b)%20%5Cpropto%20%5Cexp%5C!%5Cleft(-%5Cfrac%7B(%5Clog_2%20n%20-%20%5Cmu)%5E2%7D%7B2%5Csigma%5E2%7D%5Cright).%0A%5Ctag%7B3%7D"></span></p>
<p>If the output is actively steered by log arithmetic, the mean parameter <img src="https://latex.codecogs.com/png.latex?%5Cmu"> should scale symmetrically with <img src="https://latex.codecogs.com/png.latex?%5Clog_2(a%20%5Ccdot%20b)">. If the axis is behaviorally inert, <img src="https://latex.codecogs.com/png.latex?%5Cmu"> will show zero statistical relationship to the true product.</p>
<p>The empirical tracking sits precisely between these extremes:</p>
<ul>
<li><strong>Accuracy:</strong> 0% of the raw outputs match the correct arithmetic answer; small-scale language models cannot natively multiply integers.</li>
<li><strong>Bias:</strong> The fitted center <img src="https://latex.codecogs.com/png.latex?%5Cmu"> is systematically shifted <img src="https://latex.codecogs.com/png.latex?%5Cmathbf%7B-3.25%7D"> log-units below the true value, underestimating products by a factor of roughly <img src="https://latex.codecogs.com/png.latex?9.5%5Ctimes">.</li>
<li><strong>Coupling:</strong> Despite the severe downward bias, the scaling slope of <img src="https://latex.codecogs.com/png.latex?%5Cmu"> relative to the true values of <img src="https://latex.codecogs.com/png.latex?%5Clog_2(a%20%5Ccdot%20b)"> maintains a continuous, positive relationship at <img src="https://latex.codecogs.com/png.latex?%5Cboldsymbol%7B%5Cbeta%20=%200.22%7D">.</li>
</ul>
<p>Evaluating Pythia-70M reveals an identical scaling slope of <img src="https://latex.codecogs.com/png.latex?%5Cbeta%20=%200.22">, while Pythia-160M stabilizes at <img src="https://latex.codecogs.com/png.latex?%5Cbeta%20=%200.19">. Even though these models feature completely different output projection geometries (spanning orders of magnitude in explicit log-axis alignment), <strong>their behavioral output tracking remains uniform</strong>. The scaling limit is determined by the broader contextual frequency distributions shared across text training data rather than localized differences in projection head alignment.</p>
<table class="caption-top table">
<thead>
<tr class="header">
<th>Model</th>
<th>Log-Axis Projection Share</th>
<th>Output Scaling Slope <img src="https://latex.codecogs.com/png.latex?%5Cbeta"></th>
</tr>
</thead>
<tbody>
<tr class="odd">
<td>GPT-2 small</td>
<td>3.6% (28× baseline)</td>
<td>0.22</td>
</tr>
<tr class="even">
<td>Pythia-70M</td>
<td>0.19% (1.0× baseline)</td>
<td>0.22</td>
</tr>
<tr class="odd">
<td>Pythia-160M</td>
<td>0.01% (0.1× baseline)</td>
<td>0.19</td>
</tr>
</tbody>
</table>
<div id="fig-near-miss" class="quarto-float quarto-figure quarto-figure-center anchored">
<figure class="quarto-float quarto-float-fig figure">
<div aria-describedby="fig-near-miss-caption-0ceaefa1-69ba-4598-a22c-09a6ac19f8ca">
<img src="https://hli1234.github.io/probing-small-lms/posts/log_shortcut_findings/probe_gpt2_near_miss.png" class="img-fluid figure-img">
</div>
<figcaption class="quarto-float-caption-bottom quarto-float-caption quarto-float-fig" id="fig-near-miss-caption-0ceaefa1-69ba-4598-a22c-09a6ac19f8ca">
Figure&nbsp;9: Near-miss arithmetic for GPT-2. <strong>Left:</strong> fitted mean log of the predicted distribution vs true <img src="https://latex.codecogs.com/png.latex?%5Clog_2(a%20%5Ccdot%20b)">. The mean predicted log tracks the true log with slope <img src="https://latex.codecogs.com/png.latex?%5Cbeta%20=%200.22">, biased about 3 units below the <img src="https://latex.codecogs.com/png.latex?y%20=%20x"> line. <strong>Middle:</strong> fitted spread <img src="https://latex.codecogs.com/png.latex?%5Csigma"> is roughly constant at <img src="https://latex.codecogs.com/png.latex?%5Csim%201.9"> log-units. <strong>Right:</strong> three example predicted distributions; dotted vertical lines mark the true <img src="https://latex.codecogs.com/png.latex?%5Clog_2(a%20%5Ccdot%20b)">.
</figcaption>
</figure>
</div>
<p>This behavioral slope unifies the structural observations. The model successfully calculates <img src="https://latex.codecogs.com/png.latex?%5Clog_2(a%20%5Ccdot%20b)"> within its internal representations (<img src="https://latex.codecogs.com/png.latex?R%5E2%20=%200.989">). The generation layer responds to this signal at an effective scale of ~22% relative to a perfect readout, though it remains largely masked by stronger text-frequency constraints. The underlying circuitry is present, but its execution is heavily muted.</p>
</section>
</section>
<section id="summary-of-findings" class="level2">
<h2 class="anchored" data-anchor-id="summary-of-findings">Summary of findings</h2>
<ol type="1">
<li><strong>Logarithmic Encodings at Layer 0:</strong> The embedding layer across all evaluated models represents integer features along a robust logarithmic axis, independent of separate coordinate paths tracking parity and final digits. Surface-level linguistic markers dominate organization, whereas deeper arithmetic qualities like primality do not form coherent vector alignments.</li>
<li><strong>Causal Operand Patching:</strong> Structured modifications targeted along the input log axis exert direct, predictable changes on downstream token probabilities, whereas equivalent changes along random control axes have no effect. This confirms that the discovered directions represent active semantic scales rather than passive statistical noise.</li>
<li><strong>Uniform Behavioral Scaling:</strong> Across models with fundamentally different output connectivity, the underlying logarithmic representation consistently biases token generation at a stable tracking ratio of <img src="https://latex.codecogs.com/png.latex?%5Cbeta%20%5Capprox%200.22">. This suggests the output geometry is bounded by properties of the textual data distribution rather than localized architecture choices.</li>
<li><strong>The Probing Disconnection:</strong> While the combined scale of an arithmetic product is highly decodable at the final token position (<img src="https://latex.codecogs.com/png.latex?R%5E2%20=%200.989">), directional patching along this output axis fails to steer generation performance (causal slope = 0.07). The linear probe tracks a real internal representation, but isolates a direction that is not causally utilized by the final token generation layers.</li>
</ol>
<p>This demonstrates that small-scale transformers construct complex semantic representations whose behavioral expressions remain mostly suppressed by the dominant statistical distributions of their training text. An inability to output correct mathematical calculations does not imply the absolute absence of arithmetic circuits; rather, these underlying structures are simply overridden by louder linguistic priors.</p>
</section>
<section id="limitations" class="level2">
<h2 class="anchored" data-anchor-id="limitations">Limitations</h2>
<ul>
<li><strong>Weight-Tying Dependencies:</strong> Because GPT-2 small ties its input and output weight matrices, a significant portion of its projection alignment is an architectural artifact. While evaluating untied Pythia variants mitigates this, mapping these circuits across larger, diverse model families remains necessary.</li>
<li><strong>Logit-Space vs.&nbsp;Token Selections:</strong> The causal patching experiments measure shifts across continuous relative logits rather than forcing discrete token replacements. Because these small architectures achieve an absolute arithmetic accuracy of 0%, testing these interventions on scales that can successfully calculate products (e.g., Qwen or Llama variants) would provide clearer behavioral validation.</li>
<li><strong>Extrapolation Limits:</strong> The output-side probe performance was verified across a limited operational span (<img src="https://latex.codecogs.com/png.latex?(a,%20b)%20%5Cin%20%5B2,%2064%5D%5E2">). Whether these logarithmic vector representations scale cleanly when processing significantly larger numeric ranges remains unverified.</li>
</ul>
</section>
<section id="future-research-directions" class="level2">
<h2 class="anchored" data-anchor-id="future-research-directions">Future research directions</h2>
<ul>
<li><strong>Isolating Causal Output Axes:</strong> Determining the exact vector spaces utilized by the prediction head to route numerical selections is a critical step. Applying sparse autoencoders or singular value decompositions to numeric projection columns should expose the precise directions driving token choices.</li>
<li><strong>Validating via Exponentiation Prompts:</strong> Testing contextual calculations such as power routing (<code>a^b =</code>) can provide a more rigorous validation of the logarithmic layout. Patching the input vector by a factor of <img src="https://latex.codecogs.com/png.latex?%5CDelta"> should mathematically scale downstream outputs by <img src="https://latex.codecogs.com/png.latex?b%20%5Ccdot%20%5CDelta">.</li>
<li><strong>Cross-Layer Circuit Tracing:</strong> Identifying the specific intermediate attention heads responsible for shifting operand features from their initial inputs to the final output token positions. The existing patching framework can be extended to implement layer-by-layer path patching.</li>
</ul>
</section>
<section id="methodological-note" class="level2">
<h2 class="anchored" data-anchor-id="methodological-note">Methodological note</h2>
<p>When a linear probe optimized at a specific token position fails to resolve information when applied to a different position, it does not prove the feature is absent; the network may simply be utilizing an unaligned vector space. Optimizing a localized probe at the exact point of evaluation is a vital step in distinguishing between a missing circuit and an unaligned coordinate mapping.</p>
<hr>
</section>
<section id="code" class="level2">
<h2 class="anchored" data-anchor-id="code">Code</h2>
<p>The complete interpretive pipeline can be reproduced locally on standard hardware using the following script layout:</p>
<table class="caption-top table">
<colgroup>
<col style="width: 33%">
<col style="width: 33%">
<col style="width: 33%">
</colgroup>
<thead>
<tr class="header">
<th>Script File</th>
<th>Target Evaluation</th>
<th>Associated Reference</th>
</tr>
</thead>
<tbody>
<tr class="odd">
<td><code>reasoning_router.py</code></td>
<td>Cost-routing optimization sweep</td>
<td>Figure&nbsp;2</td>
</tr>
<tr class="even">
<td><code>probe_gpt2_log.py</code> | <code>probe_gpt2_controls.py</code></td>
<td>Embedding scale and attribute probes</td>
<td>Figure&nbsp;3</td>
</tr>
<tr class="odd">
<td><code>probe_gpt2_shared_axis.py</code></td>
<td>Cross-category numeric transfer evaluations</td>
<td>Figure&nbsp;4</td>
</tr>
<tr class="even">
<td><code>probe_gpt2_patching.py</code></td>
<td>Operand-side causal patching sweeps</td>
<td>Figure&nbsp;5</td>
</tr>
<tr class="odd">
<td><code>probe_gpt2_lmhead.py</code></td>
<td>Output matrix projection decompositions</td>
<td>Figure&nbsp;8</td>
</tr>
<tr class="even">
<td><code>probe_gpt2_pipeline.py</code></td>
<td>End-to-end product tracking checks</td>
<td>Figure&nbsp;6</td>
</tr>
<tr class="odd">
<td><code>probe_gpt2_answer_side_causal.py</code></td>
<td>Output-side causal validation loops</td>
<td>Figure&nbsp;7</td>
</tr>
<tr class="even">
<td><code>probe_gpt2_near_miss.py</code></td>
<td>Behavioral log-Gaussian distribution tracking</td>
<td>Figure&nbsp;9</td>
</tr>
<tr class="odd">
<td><code>probe_pythia_compare.py</code></td>
<td>Cross-model architectural validations</td>
<td>Data for Figure&nbsp;1</td>
</tr>
</tbody>
</table>


<!-- -->

</section>

 ]]></description>
  <category>interpretability</category>
  <category>mech-interp</category>
  <category>language models</category>
  <category>arithmetic</category>
  <guid>https://hli1234.github.io/probing-small-lms/posts/log_shortcut_findings/</guid>
  <pubDate>Fri, 03 Apr 2026 07:00:00 GMT</pubDate>
</item>
<item>
  <title>Transformers learn to anchor: a small experiment in positional periodicity</title>
  <dc:creator>Huangxing Li</dc:creator>
  <link>https://hli1234.github.io/probing-small-lms/posts/anchoring-blog/</link>
  <description><![CDATA[ 





<p>When a transformer is trained on data with a periodic structure, its attention patterns frequently exhibit sharp spikes at integer multiples of the period — 24 steps, 48 steps, 72 steps. The conventional interpretation is that the model has learned the underlying periodicity of the data and is actively retrieving relevant past events.</p>
<p>However, an alternative mechanism is possible: the model may have developed a fixed <em>positional</em> routine, learning to attend to specific slots regardless of their actual content. In this scenario, periodic spikes do not indicate that specific historical events are uniquely informative; instead, they function as an internal indexing scheme that happens to match the data’s period.</p>
<p>These two hypotheses imply very different things about what attention weights actually represent. In production environments, such as recommendation systems, distinguishing between them is difficult because you cannot manipulate the data stream without retraining, and the true importance of individual features is often unknown. To isolate these mechanisms, we can evaluate a transformer in a fully controlled, synthetic environment.</p>
<section id="the-setup" class="level2">
<h2 class="anchored" data-anchor-id="the-setup">The setup</h2>
<p>We train a small transformer (≈29k parameters, 2 layers, 1 attention head) to predict the next value of a noisy periodic signal:</p>
<p><img src="https://latex.codecogs.com/png.latex?y_t%20=%20%5Ctext%7Bintercept%7D%20+%20%5Ctext%7Bslope%7D%20%5Ccdot%20t%20+%20A%20%5Csin(2%5Cpi%20t%20/%2024%20+%20%5Cphi)%20+%20%5Cvarepsilon_t"></p>
<p>The amplitude, phase, slope, and additive noise are randomized across sequences. The model receives the past 96 values (corresponding to 4 full periods) as input and predicts the immediate next value.</p>
<div id="fig-data" class="quarto-float quarto-figure quarto-figure-center anchored">
<figure class="quarto-float quarto-float-fig figure">
<div aria-describedby="fig-data-caption-0ceaefa1-69ba-4598-a22c-09a6ac19f8ca">
<img src="https://hli1234.github.io/probing-small-lms/posts/anchoring-blog/figures/fig0_data.png" class="img-fluid figure-img">
</div>
<figcaption class="quarto-float-caption-bottom quarto-float-caption quarto-float-fig" id="fig-data-caption-0ceaefa1-69ba-4598-a22c-09a6ac19f8ca">
Figure&nbsp;1: Example sequences from the synthetic dataset. The red dot is the prediction target.
</figcaption>
</figure>
</div>
<p>This task has a straightforward optimal solution. To predict the next step, the model must estimate the trend and the phase of the sine wave. Because the underlying sine wave is smooth, adjacent points carry nearly identical phase information. Consequently, there is no intrinsic mathematical reason why an input exactly 24 steps ago should be significantly more informative than one 23 steps ago.</p>
<p>A learnable query token is appended to the sequence, and the prediction is read exclusively from this position, mimicking a LLaTTE-style readout bottleneck. After 6 epochs of training, the model achieves a validation MSE of 0.117. This is close to the irreducible noise floor (~0.09) and well below the baseline performance of simply predicting the last observed value (0.210).</p>
</section>
<section id="what-the-attention-pattern-looks-like" class="level2">
<h2 class="anchored" data-anchor-id="what-the-attention-pattern-looks-like">What the attention pattern looks like</h2>
<p>We extract the attention weights from the query token to each input position, average them across both layers, and evaluate the mean profile across 1,000 held-out validation sequences:</p>
<div id="fig-baseline" class="quarto-float quarto-figure quarto-figure-center anchored">
<figure class="quarto-float quarto-float-fig figure">
<div aria-describedby="fig-baseline-caption-0ceaefa1-69ba-4598-a22c-09a6ac19f8ca">
<img src="https://hli1234.github.io/probing-small-lms/posts/anchoring-blog/figures/fig1_baseline_attention.png" class="img-fluid figure-img">
</div>
<figcaption class="quarto-float-caption-bottom quarto-float-caption quarto-float-fig" id="fig-baseline-caption-0ceaefa1-69ba-4598-a22c-09a6ac19f8ca">
Figure&nbsp;2: Average attention from the query token vs lag. Dashed red lines mark integer multiples of the period.
</figcaption>
</figure>
</div>
<p>The resulting profile shows clear, recurring spikes spaced roughly one period apart, alongside a strong recency bias near lag 1. This matches the expected pattern, but two specific details warrant closer inspection before running interventions:</p>
<ol type="1">
<li><p><strong>The anchors deviate slightly from the true period.</strong> The peaks occur at lags of 23, 45, and 69, rather than 24, 48, and 72. This discrepancy suggests the model is not simply transcribing the data’s period. A likely explanation is that causal attention favors looking further into the past; lag 23 provides nearly identical phase information to lag 24 but extends one step deeper into the history. This indicates the spike locations are a learned structural artifact rather than a direct reflection of the data structure.</p></li>
<li><p><strong>Lag 23 receives roughly 13× more attention than lag 22.</strong> A zoom-in:</p></li>
</ol>
<div id="fig-zoom" class="quarto-float quarto-figure quarto-figure-center anchored">
<figure class="quarto-float quarto-float-fig figure">
<div aria-describedby="fig-zoom-caption-0ceaefa1-69ba-4598-a22c-09a6ac19f8ca">
<img src="https://hli1234.github.io/probing-small-lms/posts/anchoring-blog/figures/fig2_zoomed.png" class="img-fluid figure-img">
</div>
<figcaption class="quarto-float-caption-bottom quarto-float-caption quarto-float-fig" id="fig-zoom-caption-0ceaefa1-69ba-4598-a22c-09a6ac19f8ca">
Figure&nbsp;3: Zoomed view around the lag-23 spike. The dashed red line is the literal period; the spike is one step before it.
</figcaption>
</figure>
</div>
<p>Despite these two adjacent positions holding nearly identical content, their attention weights differ by an order of magnitude. We can exploit this variance to test the model’s behavior.</p>
<p>At this stage, the observations are compatible with both hypotheses: the model might attend to lag 23 because it associates that relative position with predictive utility, or it might be executing a rigid positional heuristic.</p>
</section>
<section id="intervention-1-shift-the-period-watch-the-attention" class="level2">
<h2 class="anchored" data-anchor-id="intervention-1-shift-the-period-watch-the-attention">Intervention 1: shift the period, watch the attention</h2>
<p>If the model dynamically tracks the periodic signal within the data, shifting the alignment of the period should cause the attention spikes to shift accordingly. To test this, we generate sequences where the prediction target is positioned 4 steps after the input window (rather than 1 step after, as seen during training). This shifts the optimal lookback point from lag 24 to lag 21 relative to the end of the input window. If the model is content-driven, the spike should move to lag 21. If it relies on fixed positions, the spike should remain stationary.</p>
<div id="fig-offset" class="quarto-float quarto-figure quarto-figure-center anchored">
<figure class="quarto-float quarto-float-fig figure">
<div aria-describedby="fig-offset-caption-0ceaefa1-69ba-4598-a22c-09a6ac19f8ca">
<img src="https://hli1234.github.io/probing-small-lms/posts/anchoring-blog/figures/fig4_offset.png" class="img-fluid figure-img">
</div>
<figcaption class="quarto-float-caption-bottom quarto-float-caption quarto-float-fig" id="fig-offset-caption-0ceaefa1-69ba-4598-a22c-09a6ac19f8ca">
Figure&nbsp;4: Attention pattern under baseline (predict t+1) and offset target (predict t+4). The two curves overlap completely.
</figcaption>
</figure>
</div>
<p>The baseline and offset attention curves overlap entirely, showing identical values to roughly three decimal places at every lag. The attention distribution is completely unresponsive to changes in where the period actually aligns relative to the target.</p>
<p>As a consequence of this rigidity, the prediction MSE degrades from 0.117 to 0.403—a 3.4× increase in error. The model incurs a substantial performance penalty because it cannot adapt its attention spikes to the new alignment. This result rules out the hypothesis that the attention pattern dynamically tracks the signal’s period.</p>
</section>
<section id="intervention-2-swap-the-contents" class="level2">
<h2 class="anchored" data-anchor-id="intervention-2-swap-the-contents">Intervention 2: swap the contents</h2>
<p>Even if the model uses a fixed positional routine, it might still treat the value at that specific anchor position as the primary informative feature. We can test whether the model is sensitive to the content of the anchor slot by manipulating the values at lags 22 and 23. Because the autocorrelation between adjacent points in a sine wave with a period of 24 is high (~0.97), these slots contain very similar signal profiles. We perform two distinct interventions:</p>
<ul>
<li><strong>Swap.</strong> Exchange the values at lag 22 and lag 23. Position 23 now contains the value originally at position 22, and vice versa. If the attention mechanism targets the underlying content, the peak weight should shift to position 22.</li>
<li><strong>Corrupt.</strong> Replace the value at lag 23 with random Gaussian noise. If attention depends on feature relevance, the weight at this position should drop significantly.</li>
</ul>
<div id="fig-swap" class="quarto-float quarto-figure quarto-figure-center anchored">
<figure class="quarto-float quarto-float-fig figure">
<div aria-describedby="fig-swap-caption-0ceaefa1-69ba-4598-a22c-09a6ac19f8ca">
<img src="https://hli1234.github.io/probing-small-lms/posts/anchoring-blog/figures/fig5_content_swap.png" class="img-fluid figure-img">
</div>
<figcaption class="quarto-float-caption-bottom quarto-float-caption quarto-float-fig" id="fig-swap-caption-0ceaefa1-69ba-4598-a22c-09a6ac19f8ca">
Figure&nbsp;5: Three attention curves — baseline, after value swap, and after value corruption — all overlap.
</figcaption>
</figure>
</div>
<p>All three interventions yield identical attention profiles down to the third decimal place:</p>
<div id="tbl-attn" class="quarto-float quarto-figure quarto-figure-center anchored">
<figure class="quarto-float quarto-float-tbl figure">
<figcaption class="quarto-float-caption-top quarto-float-caption quarto-float-tbl" id="tbl-attn-caption-0ceaefa1-69ba-4598-a22c-09a6ac19f8ca">
Table&nbsp;1: Attention weights at the anchor position and its neighbor under each intervention.
</figcaption>
<div aria-describedby="tbl-attn-caption-0ceaefa1-69ba-4598-a22c-09a6ac19f8ca">
<table class="caption-top table">
<thead>
<tr class="header">
<th>Position</th>
<th>Baseline attention</th>
<th>After swap</th>
<th>After corruption</th>
</tr>
</thead>
<tbody>
<tr class="odd">
<td>Lag 23 (anchor)</td>
<td>0.0625</td>
<td>0.0630</td>
<td>0.0641</td>
</tr>
<tr class="even">
<td>Lag 22 (neighbor)</td>
<td>0.0049</td>
<td>0.0049</td>
<td>—</td>
</tr>
</tbody>
</table>
</div>
</figure>
</div>
<p>Notably, attention at the anchor position increases slightly after corruption. The model fails to redirect attention away from the degraded slot; instead, it enforces its positional routine regardless of data quality.</p>
<p>The impact on prediction error reflects this rigidity:</p>
<div id="tbl-mse" class="quarto-float quarto-figure quarto-figure-center anchored">
<figure class="quarto-float quarto-float-tbl figure">
<figcaption class="quarto-float-caption-top quarto-float-caption quarto-float-tbl" id="tbl-mse-caption-0ceaefa1-69ba-4598-a22c-09a6ac19f8ca">
Table&nbsp;2: Prediction MSE under each intervention. The model cannot route around a broken anchor.
</figcaption>
<div aria-describedby="tbl-mse-caption-0ceaefa1-69ba-4598-a22c-09a6ac19f8ca">
<table class="caption-top table">
<thead>
<tr class="header">
<th>Condition</th>
<th>MSE</th>
<th>Δ from baseline</th>
</tr>
</thead>
<tbody>
<tr class="odd">
<td>Baseline</td>
<td>0.1168</td>
<td>—</td>
</tr>
<tr class="even">
<td>Value swap at lag 22 / 23</td>
<td>0.1173</td>
<td>+0.0005</td>
</tr>
<tr class="odd">
<td>Corruption of lag-23 value</td>
<td>0.1428</td>
<td>+0.0260</td>
</tr>
</tbody>
</table>
</div>
</figure>
</div>
<p>The swap causes negligible harm because the exchanged values are highly correlated and read through the same structural anchor. However, corrupting lag 23 noticeably increases the error. Because the model continues to attend heavily to the corrupted slot, it incorporates pure noise into its final prediction, demonstrating an inability to route around a broken anchor.</p>
</section>
<section id="we-learnt-and-carefully-not-learnt" class="level2">
<h2 class="anchored" data-anchor-id="we-learnt-and-carefully-not-learnt">We learnt (and carefully not learnt)</h2>
<p>In this synthetic environment, the model strictly exhibits positional anchoring rather than content-driven retrieval. The findings are supported by several points:</p>
<ul>
<li>The attention spikes do not align with the true physical period of the data (lag 23 vs.&nbsp;lag 24).</li>
<li>The spikes remain fixed when the underlying period of the data shifts.</li>
<li>The attention weights do not change when the contents of the target positions are swapped or replaced with noise.</li>
<li>The model accepts a significant increase in prediction error rather than shifting its attention weights.</li>
</ul>
<p>However, these findings come with specific caveats:</p>
<ol type="1">
<li><p><strong>This does not disprove content-driven attention in production systems.</strong> Real-world user behavior contains actual periodic routines — people do click ads at the same time of day. Models trained on complex data likely combine positional anchoring with genuine content-driven features. The experiment demonstrates that anchoring can emerge purely from positional cues, but it does not measure the balance between anchoring and content selection in large-scale models.</p></li>
<li><p><strong>The synthetic dataset is simpler than real-world data.</strong> A signal defined by a single dominant frequency and Gaussian noise makes a fixed “anchor and reconstruct” strategy highly cost-effective. Real-world sequences feature irregular spacing, multiple overlapping time horizons, and heterogeneous content, which changes the trade-offs and likely reduces the severity of pure anchoring.</p></li>
<li><p><strong>The scope is limited to a single model instance.</strong> We did not perform a broad sweep of architectures or initialization seeds. While the presence of positional anchors was robust enough to appear clearly on the first run, the exact lag of the spikes should be viewed as an implementation detail of this specific training instance.</p></li>
<li><p><strong>The anchoring behavior dictates downstream performance.</strong> Corrupting the anchor directly degrades the final MSE because the model cannot adjust. This highlights why interpreting attention weights as a proxy for “feature importance” can be misleading: the model treats the slot as important even when its contents are demonstrably harmful to the prediction.</p></li>
</ol>
</section>
<section id="the-takeaway" class="level2">
<h2 class="anchored" data-anchor-id="the-takeaway">The takeaway</h2>
<p>I find this useful as a calibration. The next time someone shows me an attention plot with sharp periodic spikes and a story about temporal patterns, I’ll want to know: did anyone test whether the spikes move when the periodic alignment moves? Did anyone swap the contents of the spike positions? If not, the plot is consistent with the model having learned a clever indexing scheme that has nothing to do with the temporal narrative.</p>
<p>It’s also a reminder that interpretability is hard in a specific way. The attention pattern isn’t telling us what the model thinks; it’s telling us what the model <em>does</em>. Translating from “does” to “thinks” requires interventions, and in this small example the two come out genuinely different.</p>


<!-- -->

</section>

 ]]></description>
  <category>interpretability</category>
  <category>transformers</category>
  <category>experiments</category>
  <guid>https://hli1234.github.io/probing-small-lms/posts/anchoring-blog/</guid>
  <pubDate>Wed, 10 Dec 2025 08:00:00 GMT</pubDate>
  <media:content url="https://hli1234.github.io/probing-small-lms/posts/anchoring-blog/figures/fig1_baseline_attention.png" medium="image" type="image/png" height="58" width="144"/>
</item>
</channel>
</rss>
