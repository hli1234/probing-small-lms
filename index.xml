<?xml version="1.0" encoding="UTF-8"?>
<rss  xmlns:atom="http://www.w3.org/2005/Atom" 
      xmlns:media="http://search.yahoo.com/mrss/" 
      xmlns:content="http://purl.org/rss/1.0/modules/content/" 
      xmlns:dc="http://purl.org/dc/elements/1.1/" 
      version="2.0">
<channel>
<title>Probing small LMs</title>
<link>https://hli1234.github.io/probing-small-lms/</link>
<atom:link href="https://hli1234.github.io/probing-small-lms/index.xml" rel="self" type="application/rss+xml"/>
<description>Probes, patches, and pictures from the residual stream.</description>
<generator>quarto-1.9.37</generator>
<lastBuildDate>Sun, 17 May 2026 07:00:00 GMT</lastBuildDate>
<item>
  <title>Transformers learn to anchor: a small experiment in positional periodicity</title>
  <dc:creator>Huangxing Li</dc:creator>
  <link>https://hli1234.github.io/probing-small-lms/posts/anchoring-blog/</link>
  <description><![CDATA[ 





<p>When you train a transformer on data that has a periodic structure, its attention pattern often shows striking spikes at integer multiples of the period — 24 steps, 48 steps, 72 steps. The natural reading is that the model has learned the periodicity of the data and is “looking at” the corresponding earlier events.</p>
<p>But there’s a quieter, weirder possibility. The model could instead have compiled a fixed <em>positional</em> routine: “always look at slot 24, regardless of what’s there.” Under this story, the periodic spikes aren’t reports about which events are informative — they’re an internal indexing scheme that happens to align with the period of the data.</p>
<p>The two stories make very different predictions about what attention “means,” and they’re hard to tell apart in a real recommendation system, where you can’t manipulate the data without retraining and you don’t know which events actually matter. So let’s set the real system aside and run the experiment in a sandbox where we control everything.</p>
<section id="the-setup" class="level2">
<h2 class="anchored" data-anchor-id="the-setup">The setup</h2>
<p>We train a tiny transformer (≈29k parameters, 2 layers, 1 attention head) to predict the next value of a noisy periodic signal:</p>
<p><img src="https://latex.codecogs.com/png.latex?y_t%20=%20%5Ctext%7Bintercept%7D%20+%20%5Ctext%7Bslope%7D%20%5Ccdot%20t%20+%20A%20%5Csin(2%5Cpi%20t%20/%2024%20+%20%5Cphi)%20+%20%5Cvarepsilon_t"></p>
<p>with random amplitude, phase, slope, and additive noise. The model sees the last 96 values (4 full periods) and predicts the next one.</p>
<div id="fig-data" class="quarto-float quarto-figure quarto-figure-center anchored">
<figure class="quarto-float quarto-float-fig figure">
<div aria-describedby="fig-data-caption-0ceaefa1-69ba-4598-a22c-09a6ac19f8ca">
<img src="https://hli1234.github.io/probing-small-lms/posts/anchoring-blog/figures/fig0_data.png" class="img-fluid figure-img">
</div>
<figcaption class="quarto-float-caption-bottom quarto-float-caption quarto-float-fig" id="fig-data-caption-0ceaefa1-69ba-4598-a22c-09a6ac19f8ca">
Figure&nbsp;1: Example sequences from the synthetic dataset. The red dot is the prediction target.
</figcaption>
</figure>
</div>
<p>This task has clean optimal behavior. To predict the next value you need to estimate the trend and the phase of the sine. To estimate phase, <em>any</em> point in the past is informative once you know its position relative to the target — and adjacent points are nearly equally informative because the underlying sine is smooth. There’s no special reason for “exactly 24 steps ago” to matter more than “23 steps ago.”</p>
<p>A learnable query token is appended to the sequence; the model reads its prediction only from that position, mirroring the LLaTTE-style readout bottleneck. After 6 epochs of training the model reaches validation MSE 0.117, close to the irreducible noise floor (~0.09), and well below the predict-last-value baseline (0.210).</p>
</section>
<section id="what-the-attention-pattern-looks-like" class="level2">
<h2 class="anchored" data-anchor-id="what-the-attention-pattern-looks-like">What the attention pattern looks like</h2>
<p>We extract attention weights from the query token back to each input position, average across the layers, and then average across 1000 held-out sequences:</p>
<div id="fig-baseline" class="quarto-float quarto-figure quarto-figure-center anchored">
<figure class="quarto-float quarto-float-fig figure">
<div aria-describedby="fig-baseline-caption-0ceaefa1-69ba-4598-a22c-09a6ac19f8ca">
<img src="https://hli1234.github.io/probing-small-lms/posts/anchoring-blog/figures/fig1_baseline_attention.png" class="img-fluid figure-img">
</div>
<figcaption class="quarto-float-caption-bottom quarto-float-caption quarto-float-fig" id="fig-baseline-caption-0ceaefa1-69ba-4598-a22c-09a6ac19f8ca">
Figure&nbsp;2: Average attention from the query token vs lag. Dashed red lines mark integer multiples of the period.
</figcaption>
</figure>
</div>
<p>There it is. Sharp recurring spikes, roughly one period apart, plus a strong recency peak near lag 1. The model has learned positional anchoring.</p>
<p>A couple of details worth flagging now, since they’ll matter for the interventions:</p>
<ol type="1">
<li><p><strong>The anchors are slightly off the literal period.</strong> The peaks sit at lags 23, 45, 69, not 24, 48, 72. This is itself a hint of B: the data’s period is exactly 24, but the model picked something else. Why? My best guess: causal attention favors looking <em>into</em> the past rather than <em>at</em> the period boundary; lag 23 gives almost the same phase information as lag 24 but is one step deeper into the predictive horizon. Either way, the discrepancy means the spike locations are a learned implementation detail, not a transcription of the data’s period.</p></li>
<li><p><strong>Lag 23 gets ~13× more attention than lag 22.</strong> A zoom-in:</p></li>
</ol>
<div id="fig-zoom" class="quarto-float quarto-figure quarto-figure-center anchored">
<figure class="quarto-float quarto-float-fig figure">
<div aria-describedby="fig-zoom-caption-0ceaefa1-69ba-4598-a22c-09a6ac19f8ca">
<img src="https://hli1234.github.io/probing-small-lms/posts/anchoring-blog/figures/fig2_zoomed.png" class="img-fluid figure-img">
</div>
<figcaption class="quarto-float-caption-bottom quarto-float-caption quarto-float-fig" id="fig-zoom-caption-0ceaefa1-69ba-4598-a22c-09a6ac19f8ca">
Figure&nbsp;3: Zoomed view around the lag-23 spike. The dashed red line is the literal period; the spike is one step before it.
</figcaption>
</figure>
</div>
<p>Two adjacent positions, near-identical content, very different attention weight. This is the configuration the swap experiment will exploit.</p>
<p>So far everything is consistent with both interpretations. The model could be paying attention at lag 23 because it has learned that the period-aligned point is informative (A), or because it has compiled a positional reading routine (B). Time to disambiguate.</p>
</section>
<section id="intervention-1-shift-the-period-watch-the-attention" class="level2">
<h2 class="anchored" data-anchor-id="intervention-1-shift-the-period-watch-the-attention">Intervention 1: shift the period, watch the attention</h2>
<p>If the model is tracking the periodic signal in the data, then changing where the period actually aligns should move the attention spikes. Concretely: we generate sequences where the prediction target sits 4 steps after the input window (instead of 1 step after, as in training). Now the “one period before target” point sits at lag 21 from the end of the input window, not lag 24. Under A, the spike should follow the signal. Under B, the spike stays where the model put it.</p>
<div id="fig-offset" class="quarto-float quarto-figure quarto-figure-center anchored">
<figure class="quarto-float quarto-float-fig figure">
<div aria-describedby="fig-offset-caption-0ceaefa1-69ba-4598-a22c-09a6ac19f8ca">
<img src="https://hli1234.github.io/probing-small-lms/posts/anchoring-blog/figures/fig4_offset.png" class="img-fluid figure-img">
</div>
<figcaption class="quarto-float-caption-bottom quarto-float-caption quarto-float-fig" id="fig-offset-caption-0ceaefa1-69ba-4598-a22c-09a6ac19f8ca">
Figure&nbsp;4: Attention pattern under baseline (predict t+1) and offset target (predict t+4). The two curves overlap completely.
</figcaption>
</figure>
</div>
<p>You can only see one curve because the two are exactly on top of each other — the baseline and offset attention patterns are identical to ~3 decimal places at every lag. The model’s attention pattern is completely insensitive to where the period actually aligns relative to the target.</p>
<p>Meanwhile prediction MSE went from 0.117 to 0.403 — a 3.4× degradation. The model is paying a real cost for keeping its attention pinned to the wrong positions. It would benefit from moving the spikes, and it doesn’t.</p>
<p>This rules out the strongest form of A. The attention pattern is not “wherever the period-aligned points happen to be.” It’s wherever the model has learned to look.</p>
</section>
<section id="intervention-2-swap-the-contents" class="level2">
<h2 class="anchored" data-anchor-id="intervention-2-swap-the-contents">Intervention 2: swap the contents</h2>
<p>OK, but maybe the model has just learned a fixed positional routine that <em>usually</em> aligns with the period and treats the value at that position as informative. In that case, the value at the anchor position should still matter to the model. We can test this by leaving the anchor position alone but changing what value sits there.</p>
<p>The anchor is at lag 23. Its near neighbor at lag 22 carries essentially the same information (autocorrelation between adjacent points in a sine with period 24 is ~0.97). We do two interventions:</p>
<ul>
<li><strong>Swap.</strong> Exchange the values at lag 22 and lag 23. Position 23 now holds what used to be at position 22 (similar but not identical value); position 22 now holds the old anchor value. If attention is following informative content, it should move to position 22.</li>
<li><strong>Corrupt.</strong> Replace the value at lag 23 with random Gaussian noise. If attention reflects informativeness, it should drop (the value is now uninformative).</li>
</ul>
<div id="fig-swap" class="quarto-float quarto-figure quarto-figure-center anchored">
<figure class="quarto-float quarto-float-fig figure">
<div aria-describedby="fig-swap-caption-0ceaefa1-69ba-4598-a22c-09a6ac19f8ca">
<img src="https://hli1234.github.io/probing-small-lms/posts/anchoring-blog/figures/fig5_content_swap.png" class="img-fluid figure-img">
</div>
<figcaption class="quarto-float-caption-bottom quarto-float-caption quarto-float-fig" id="fig-swap-caption-0ceaefa1-69ba-4598-a22c-09a6ac19f8ca">
Figure&nbsp;5: Three attention curves — baseline, after value swap, and after value corruption — all overlap.
</figcaption>
</figure>
</div>
<p>Three curves, one shape. The attention pattern is unchanged under both interventions, down to the third decimal. Numerically:</p>
<div id="tbl-attn" class="quarto-float quarto-figure quarto-figure-center anchored">
<figure class="quarto-float quarto-float-tbl figure">
<figcaption class="quarto-float-caption-top quarto-float-caption quarto-float-tbl" id="tbl-attn-caption-0ceaefa1-69ba-4598-a22c-09a6ac19f8ca">
Table&nbsp;1: Attention weights at the anchor position and its neighbor under each intervention.
</figcaption>
<div aria-describedby="tbl-attn-caption-0ceaefa1-69ba-4598-a22c-09a6ac19f8ca">
<table class="caption-top table">
<thead>
<tr class="header">
<th>Position</th>
<th>Baseline attention</th>
<th>After swap</th>
<th>After corruption</th>
</tr>
</thead>
<tbody>
<tr class="odd">
<td>Lag 23 (anchor)</td>
<td>0.0625</td>
<td>0.0630</td>
<td>0.0641</td>
</tr>
<tr class="even">
<td>Lag 22 (neighbor)</td>
<td>0.0049</td>
<td>0.0049</td>
<td>—</td>
</tr>
</tbody>
</table>
</div>
</figure>
</div>
<p>Note the small <em>increase</em> in attention after corruption. The model didn’t just fail to redirect attention away from a now-meaningless slot — it slightly increased the weight. The anchor position is a routine, not a referendum on what’s there.</p>
<p>The prediction MSE tells a coherent story:</p>
<div id="tbl-mse" class="quarto-float quarto-figure quarto-figure-center anchored">
<figure class="quarto-float quarto-float-tbl figure">
<figcaption class="quarto-float-caption-top quarto-float-caption quarto-float-tbl" id="tbl-mse-caption-0ceaefa1-69ba-4598-a22c-09a6ac19f8ca">
Table&nbsp;2: Prediction MSE under each intervention. The model cannot route around a broken anchor.
</figcaption>
<div aria-describedby="tbl-mse-caption-0ceaefa1-69ba-4598-a22c-09a6ac19f8ca">
<table class="caption-top table">
<thead>
<tr class="header">
<th>Condition</th>
<th>MSE</th>
<th>Δ from baseline</th>
</tr>
</thead>
<tbody>
<tr class="odd">
<td>Baseline</td>
<td>0.1168</td>
<td>—</td>
</tr>
<tr class="even">
<td>Value swap at lag 22 / 23</td>
<td>0.1173</td>
<td>+0.0005</td>
</tr>
<tr class="odd">
<td>Corruption of lag-23 value</td>
<td>0.1428</td>
<td>+0.0260</td>
</tr>
</tbody>
</table>
</div>
</figure>
</div>
<p>The swap barely hurts (the swapped values are nearly identical, and the model reads them through the same anchor either way). Corruption hurts noticeably, because the model is <em>still reading from the corrupted position</em> and is now putting noise into its prediction. It cannot route around the broken anchor.</p>
</section>
<section id="what-this-shows-and-what-it-doesnt" class="level2">
<h2 class="anchored" data-anchor-id="what-this-shows-and-what-it-doesnt">What this shows, and what it doesn’t</h2>
<p>In this controlled setting the model very clearly does Interpretation B. Periodic attention spikes are positional anchors, not pointers at informative events. The evidence:</p>
<ul>
<li>The spikes don’t sit on the literal period (lag 23, not lag 24).</li>
<li>They don’t move when the period of the data shifts.</li>
<li>They don’t move when the values at those positions are swapped or replaced with noise.</li>
<li>The model pays a measurable prediction cost rather than redirecting attention.</li>
</ul>
<p>What this does <em>not</em> show, in order of importance:</p>
<ol type="1">
<li><p><strong>This isn’t a refutation of A in real systems.</strong> Real user behavior has genuine routines — people do click ads at the same time of day. A model trained on real data probably learns <em>both</em> a periodic anchoring strategy <em>and</em> picks up on event-level signal. The experiment shows that anchoring is a thing transformers can learn purely from positional cues; it doesn’t quantify how much of any real attention pattern is anchoring vs.&nbsp;content.</p></li>
<li><p><strong>The synthetic data is much simpler than user behavior.</strong> With a single dominant frequency and Gaussian noise, the cheapest strategy may genuinely be “anchor on the period and reconstruct.” Real sequences have multiple time scales, irregular spacing, and heterogeneous content; the cost-benefit tradeoff for anchoring is different. I’d expect anchoring to be <em>less</em> extreme there, with more content-sensitivity mixed in.</p></li>
<li><p><strong>One model, one seed, one period.</strong> I didn’t sweep architectures or seeds. The qualitative finding (positional anchors exist and dominate attention) was robust enough to show up clearly on the first training run, but the precise lag of the spikes is an implementation detail of this particular run.</p></li>
<li><p><strong>The anchoring is causally consequential.</strong> Corruption of the anchor degrades MSE; the model can’t compensate. This is the part that should make us cautious about interpreting attention as “what the model thinks is informative.” The model thinks the slot is informative — and is wrong, in a way it can’t correct.</p></li>
</ol>
</section>
<section id="the-takeaway" class="level2">
<h2 class="anchored" data-anchor-id="the-takeaway">The takeaway</h2>
<p>I find this useful as a calibration. The next time someone shows me an attention plot with sharp periodic spikes and a story about temporal patterns, I’ll want to know: did anyone test whether the spikes move when the periodic alignment moves? Did anyone swap the contents of the spike positions? If not, the plot is consistent with the model having learned a clever indexing scheme that has nothing to do with the temporal narrative.</p>
<p>It’s also a reminder that interpretability is hard in a specific way. The attention pattern isn’t telling us what the model thinks; it’s telling us what the model <em>does</em>. Translating from “does” to “thinks” requires interventions, and in this small example the two come out genuinely different.</p>


<!-- -->

</section>

 ]]></description>
  <category>interpretability</category>
  <category>transformers</category>
  <category>experiments</category>
  <guid>https://hli1234.github.io/probing-small-lms/posts/anchoring-blog/</guid>
  <pubDate>Sun, 17 May 2026 07:00:00 GMT</pubDate>
  <media:content url="https://hli1234.github.io/probing-small-lms/posts/anchoring-blog/figures/fig1_baseline_attention.png" medium="image" type="image/png" height="58" width="144"/>
</item>
<item>
  <title>Does GPT-2 know how big a galaxy is?</title>
  <dc:creator>Huangxing Li</dc:creator>
  <link>https://hli1234.github.io/probing-small-lms/posts/physics_axes/</link>
  <description><![CDATA[ 





<section id="the-setup" class="level2">
<h2 class="anchored" data-anchor-id="the-setup">The setup</h2>
<p>In an <a href="../log_shortcut_findings/log_shortcut_findings.qmd">earlier post</a> I showed that GPT-2 small’s token embedding represents integer magnitude on a roughly logarithmic axis (<img src="https://latex.codecogs.com/png.latex?R%5E2%20=%200.95"> for <img src="https://latex.codecogs.com/png.latex?%5Clog_2(x)">). The natural generalisation: does the same model represent <em>physical</em> magnitude — sizes, durations, masses — on similar axes?</p>
<p>It’s not obvious which way this should go. On one hand, GPT-2 has read text about atoms (<img src="https://latex.codecogs.com/png.latex?10%5E%7B-10%7D"> m), humans (<img src="https://latex.codecogs.com/png.latex?%5Csim%201"> m), and galaxies (<img src="https://latex.codecogs.com/png.latex?10%5E%7B21%7D"> m). A unified “small-to-big” axis would let the model encode 30 orders of magnitude in a single direction, which is efficient. On the other hand, words like <code>electron</code>, <code>kilogram</code>, <code>galaxy</code>, and <code>marathon</code> are first particles, units, celestial bodies, and races; their mass or length is a secondary attribute that the language model may or may not have factored out into a clean direction.</p>
<p>To test, I assembled three lists of physical entities with known SI values:</p>
<ul>
<li><strong>Length</strong> (19 entities, log₁₀ spanning <img src="https://latex.codecogs.com/png.latex?-10"> to <img src="https://latex.codecogs.com/png.latex?+21">): <code>atom</code>, <code>virus</code>, <code>cell</code>, <code>meter</code>, <code>inch</code>, <code>mile</code>, <code>marathon</code>, <code>earth</code>, <code>sun</code>, <code>lightyear</code>, <code>parsec</code>, <code>galaxy</code>, etc.</li>
<li><strong>Time</strong> (13 entities, log₁₀ from <img src="https://latex.codecogs.com/png.latex?-9"> to <img src="https://latex.codecogs.com/png.latex?+10.5">): <code>nanosecond</code>, <code>millisecond</code>, <code>second</code>, <code>minute</code>, <code>hour</code>, <code>day</code>, <code>month</code>, <code>year</code>, <code>decade</code>, <code>century</code>, <code>millennium</code>, etc.</li>
<li><strong>Mass</strong> (13 entities, log₁₀ from <img src="https://latex.codecogs.com/png.latex?-30"> to <img src="https://latex.codecogs.com/png.latex?+30">): <code>electron</code>, <code>proton</code>, <code>gram</code>, <code>ounce</code>, <code>pound</code>, <code>kilogram</code>, <code>ton</code>, <code>elephant</code>, <code>whale</code>, <code>earth</code>, <code>sun</code>.</li>
</ul>
<p>For each entity I take the embedding of <code>" {name}"</code> (single space prefix) and use the last token’s residual at layer 0. With small datasets I use leave-one-out cross-validation on a ridge probe.</p>
<p>The two hypotheses being tested:</p>
<ul>
<li><strong>H1 (separate axes).</strong> Each physical dimension has its own log axis, not shared with the others. Probes trained on one don’t transfer.</li>
<li><strong>H2 (unit-agnostic magnitude).</strong> There is a shared “small-to-big” direction; the same probe works across length, time, and mass.</li>
</ul>
</section>
<section id="the-result" class="level2">
<h2 class="anchored" data-anchor-id="the-result">The result</h2>
<div id="fig-physics" class="quarto-float quarto-figure quarto-figure-center anchored">
<figure class="quarto-float quarto-float-fig figure">
<div aria-describedby="fig-physics-caption-0ceaefa1-69ba-4598-a22c-09a6ac19f8ca">
<img src="https://hli1234.github.io/probing-small-lms/posts/physics_axes/probe_gpt2_physics.png" class="img-fluid figure-img">
</div>
<figcaption class="quarto-float-caption-bottom quarto-float-caption quarto-float-fig" id="fig-physics-caption-0ceaefa1-69ba-4598-a22c-09a6ac19f8ca">
Figure&nbsp;1: Probing physical-scale axes in GPT-2 small. <strong>Top:</strong> within-category scatter of leave-one-out probe predictions vs true log₁₀(value). Time tracks the diagonal closely (<img src="https://latex.codecogs.com/png.latex?R%5E2%20=%200.81">); length is messy; mass is nearly random. <strong>Bottom left:</strong> cross-category transfer matrix. Diagonal entries are within-category leave-one-out R²; off-diagonal entries are train-on-one, test-on-another. <strong>Bottom middle:</strong> combined probe across all three categories (<img src="https://latex.codecogs.com/png.latex?R%5E2%20=%200.26">). <strong>Bottom right:</strong> summary numbers.
</figcaption>
</figure>
</div>
<p>The headline numbers:</p>
<table class="caption-top table">
<thead>
<tr class="header">
<th>Category</th>
<th>Within-category log R²</th>
<th>Within-category linear R²</th>
</tr>
</thead>
<tbody>
<tr class="odd">
<td><strong>Time</strong></td>
<td><strong>0.81</strong></td>
<td><img src="https://latex.codecogs.com/png.latex?-0.07"></td>
</tr>
<tr class="even">
<td>Length</td>
<td>0.18</td>
<td><img src="https://latex.codecogs.com/png.latex?-0.19"></td>
</tr>
<tr class="odd">
<td>Mass</td>
<td>0.03</td>
<td><img src="https://latex.codecogs.com/png.latex?-0.27"></td>
</tr>
</tbody>
</table>
<p>Time is encoded on a clear log axis. Length is mostly noise. Mass is essentially random.</p>
<p>Cross-category transfer is mostly negative, with one exception: <code>time → mass</code> gives <img src="https://latex.codecogs.com/png.latex?R%5E2%20=%20+0.19">, which is barely above zero but more than the others. The combined probe across all 45 entities gives <img src="https://latex.codecogs.com/png.latex?R%5E2%20=%200.26"> — there’s <em>some</em> shared structure, but it’s weak.</p>
<p>So both hypotheses lose: there is no clean unit-agnostic axis, but neither do the per-category axes individually work well. The model has a clean log-time axis, weak log-length, no log-mass.</p>
</section>
<section id="why-time-works-and-the-others-dont" class="level2">
<h2 class="anchored" data-anchor-id="why-time-works-and-the-others-dont">Why time works and the others don’t</h2>
<p>The first thing to ask is whether time is special because of physics or because of language. I think it’s mostly language. Time words form a conventional one-dimensional ladder in English:</p>
<blockquote class="blockquote">
<p>nanosecond → microsecond → millisecond → second → minute → hour → day → week → month → year → decade → century → millennium</p>
</blockquote>
<p>These tokens are functionally interchangeable in many contexts — <code>"for five {time-word}"</code> parses for almost every entry in that ladder. The embedding can therefore afford to compress them along a single “duration” direction, because almost everything else about these words is shared (they’re all temporal units modifying durations and frequencies).</p>
<p>Length and mass don’t have an analogous unidimensional ladder. The “length words” I used are a mix of:</p>
<ul>
<li>Units of measurement: <code>millimeter</code>, <code>inch</code>, <code>meter</code>, <code>mile</code>, <code>lightyear</code>, <code>parsec</code></li>
<li>Objects characterised primarily by length scale: <code>atom</code>, <code>virus</code>, <code>cell</code>, <code>galaxy</code></li>
<li>Specific entities: <code>earth</code>, <code>sun</code>, <code>moon</code>, <code>marathon</code></li>
</ul>
<p>These belong to different semantic categories. The embedding of <code>atom</code> probably encodes “this is a kind of particle” more strongly than “this is <img src="https://latex.codecogs.com/png.latex?10%5E%7B-10%7D"> m long”. The embedding of <code>marathon</code> encodes “this is a race” more strongly than “this is 42 km”.</p>
<p>Mass is worse still. <code>electron</code>, <code>proton</code>, and <code>neutron</code> are subatomic particles; <code>gram</code>, <code>ounce</code>, <code>pound</code>, <code>kilogram</code>, <code>ton</code> are units; <code>elephant</code>, <code>whale</code> are animals; <code>earth</code>, <code>sun</code> are celestial bodies. The mass-magnitude attribute these all share is a tertiary feature, not something the embedding has factored out into a clean direction.</p>
<div class="callout callout-style-default callout-note callout-titled" title="The cleaner test, which I haven't run">
<div class="callout-header d-flex align-content-center">
<div class="callout-icon-container">
<i class="callout-icon"></i>
</div>
<div class="callout-title-container flex-fill">
<span class="screen-reader-only">Note</span>The cleaner test, which I haven’t run
</div>
</div>
<div class="callout-body-container callout-body">
<p>If this hypothesis is right, then probing mass <em>with disambiguating context</em> should rescue the result. For each entity, instead of just the bare token, use a prompt like <code>"The mass of a typical {entity} is"</code> and probe the residual at the final token. The model is then forced to think about mass, not entity identity, so the magnitude representation should be cleaner. Same trick should help length too. I’ll come back to this.</p>
</div>
</div>
</section>
<section id="what-it-tells-you-about-llm-physics-intuition" class="level2">
<h2 class="anchored" data-anchor-id="what-it-tells-you-about-llm-physics-intuition">What it tells you about LLM “physics intuition”</h2>
<p>A claim you sometimes see: large language models have learned a coherent physical world model from text. Whatever else this means, it would have to include something like a shared magnitude axis — knowing that “galactic” things are big in both length and mass, that “atomic” things are small in both, that “millennial” describes long times.</p>
<p>This experiment is the smallest possible test of that claim, and the result is: GPT-2 small has built a clean axis for <em>time</em> but not for <em>length</em> or <em>mass</em>. The unit-agnostic “small to big” axis is at best very weakly present (combined <img src="https://latex.codecogs.com/png.latex?R%5E2%20=%200.26"> across 45 entities), and the per-category transfer is mostly negative.</p>
<p>I want to be careful about how much weight this puts on the negative claim. The test uses unembellished token embeddings, no context, and a small entity list. It tests whether the LLM has the axes “for free”, at the very first layer. Almost certainly:</p>
<ul>
<li>A bigger model would do better. Pythia-1.4B and Llama would have more capacity to factor magnitude out from category.</li>
<li>A model probed with context would do better. The “mass of an elephant” context steers the residual stream toward representing mass specifically.</li>
<li>A model probed later in the network would do better. By layer 12 the transformer has integrated context with the token; that probably combines “this is an elephant” + relevant magnitude knowledge.</li>
</ul>
<p>So the right summary is: <strong>at the embedding layer, GPT-2 has built a clean log-time axis but has not factored magnitude out from length-related and mass-related token semantics</strong>. The model has time-scale intuition more readily than length-scale or mass-scale intuition, because temporal nouns in English happen to form a clean one-dimensional ladder.</p>
</section>
<section id="a-specific-prediction-and-the-test" class="level2">
<h2 class="anchored" data-anchor-id="a-specific-prediction-and-the-test">A specific prediction — and the test</h2>
<p>The hypothesis above predicts that contextualised probing should rescue length and mass. If I prompt <code>"The mass of a typical {entity} is about"</code> instead of feeding the bare entity word, then by the time the residual stream reaches the last token (<code>"about"</code>), the model has been told what quantity to think about. The magnitude information that was hiding behind category should now surface on a probe-friendly direction.</p>
<p>To test, I re-ran the experiment with context. For each category, the prompt template is <code>"The {size|duration|mass} of a typical {entity} is about"</code>, and I probe the residual stream at the last token, across all layers.</p>
<div class="callout callout-style-default callout-important callout-titled" title="Prediction confirmed">
<div class="callout-header d-flex align-content-center">
<div class="callout-icon-container">
<i class="callout-icon"></i>
</div>
<div class="callout-title-container flex-fill">
<span class="screen-reader-only">Important</span>Prediction confirmed
</div>
</div>
<div class="callout-body-container callout-body">
<p>Going from bare-token probing to contextualised probing, the mass R² jumps from <strong>0.45 to 0.84</strong> (best layer). Time goes from 0.88 to 0.95. Length from 0.30 to 0.48. The model <em>has</em> the magnitude information; the bare token just doesn’t store it on a probe-friendly direction.</p>
</div>
</div>
<div id="fig-physics-context" class="quarto-float quarto-figure quarto-figure-center anchored">
<figure class="quarto-float quarto-float-fig figure">
<div aria-describedby="fig-physics-context-caption-0ceaefa1-69ba-4598-a22c-09a6ac19f8ca">
<img src="https://hli1234.github.io/probing-small-lms/posts/physics_axes/probe_gpt2_physics_context.png" class="img-fluid figure-img">
</div>
<figcaption class="quarto-float-caption-bottom quarto-float-caption quarto-float-fig" id="fig-physics-context-caption-0ceaefa1-69ba-4598-a22c-09a6ac19f8ca">
Figure&nbsp;2: Layer-by-layer leave-one-out R² for <img src="https://latex.codecogs.com/png.latex?%5Clog_%7B10%7D(%5Ctext%7BSI%20value%7D)">, in two conditions: bare entity token (blue) versus contextualised prompt at the last token (red). For all three categories, contextualised probing reaches a higher peak R². The largest jump is for mass (0.45 → 0.84), confirming that the bare token was hiding magnitude behind semantic category. The layer profile is also informative: bare-token time peaks at layer 2 (embedding-level info), while contextualised probes peak near the final layer (context integration).
</figcaption>
</figure>
</div>
<p>The layer profile of each curve tells a small mechanistic story:</p>
<ul>
<li><strong>Bare time</strong> peaks at <em>layer 2</em> and gradually declines through later layers — the magnitude information sits in the embedding itself and gets diluted by subsequent processing. The temporal ladder really is built into how the tokenizer/embedding handles these words.</li>
<li><strong>Contextualised time, length, and mass</strong> all peak at the <em>final layer</em> (or close to it). The context-driven magnitude representation is built up across the network, not present at the embedding.</li>
<li><strong>Bare mass</strong> rises from <img src="https://latex.codecogs.com/png.latex?R%5E2%20=%200.03"> at layer 0 to <img src="https://latex.codecogs.com/png.latex?0.45"> at layer 9 before falling back. Even without context, the transformer recovers some mass-magnitude representation by mid-network — though much less than with context.</li>
</ul>
<p>The cleanest summary: GPT-2 has rich physical-magnitude knowledge across all three categories, but only time stores it <em>at the embedding layer</em>. Length and mass require context-driven processing through the layers to extract their magnitude information. The asymmetry was real, but the right diagnosis isn’t “the model doesn’t know” — it’s “the model knows but needs the right question to surface it”.</p>
</section>
<section id="whats-next" class="level2">
<h2 class="anchored" data-anchor-id="whats-next">What’s next</h2>
<p>With the context-rescue result in hand, the obvious follow-ups shift:</p>
<p><strong>Dimensional analysis.</strong> Does GPT-2 have a “units” axis? For prompts like <code>"velocity has units of"</code>, can it predict the right answer? Can the model distinguish <code>m/s</code> (a velocity) from <code>m·s</code> (a strange quantity) in its internal representations? A probe along the lines of “given the residual stream, can you predict the dimensional formula?” would test the model’s grasp of unit composition.</p>
<p><strong>Order-of-magnitude estimation.</strong> Standard Fermi questions (“How many piano tuners are there in Chicago?”) test the model’s ability to multiply rough estimates in head. If the integer-multiplication shortcut work I did earlier extends, the model should handle these via log-axis addition.</p>
<p><strong>Scale-ladder representation across modalities.</strong> If we could combine text + image embeddings (e.g., in CLIP or a multimodal LLM), is there a “scale” axis that lines up across <code>image of atom</code> and <code>image of galaxy</code>? The conjecture: in a model trained on enough multimodal data, yes, because images come with implicit scale cues (microscope, telescope, etc.).</p>
<p><strong>Test the linguistic-ladder hypothesis directly.</strong> Take a bunch of abstract concept categories where the words <em>do</em> form a clean ladder (temperature: <code>cold</code>, <code>cool</code>, <code>warm</code>, <code>hot</code>, <code>boiling</code>; speed: <code>slow</code>, <code>crawling</code>, <code>walking</code>, <code>running</code>, <code>sprinting</code>, <code>flying</code>). The probe should work cleanly on these for the same reason it worked on time. Categories without a ladder (length, mass) should fail. This isolates the linguistic mechanism.</p>
</section>
<section id="code" class="level2">
<h2 class="anchored" data-anchor-id="code">Code</h2>
<ul>
<li><code>probe_gpt2_physics.py</code> — bare-token experiment.</li>
<li><code>probe_gpt2_physics_context.py</code> — contextualised experiment, with the layer-by-layer comparison.</li>
</ul>
<p>Both reproduce in about a minute on CPU.</p>
<hr>
<p><em>The “GPT-2 knows physics” narrative needs a careful unpacking. The model has </em>all three* magnitude axes when you ask in the right way (<img src="https://latex.codecogs.com/png.latex?R%5E2%20%5Cin%20%5B0.48,%200.95%5D">). But only time has its magnitude stored right at the embedding ladder. Length and mass require the network to do context-driven work across many layers to extract the same information. The takeaway for probing methodology: a negative result from bare-token probing tells you about the embedding, not about the model’s knowledge.*</p>


<!-- -->

</section>

 ]]></description>
  <category>interpretability</category>
  <category>physics</category>
  <category>language models</category>
  <guid>https://hli1234.github.io/probing-small-lms/posts/physics_axes/</guid>
  <pubDate>Mon, 06 May 2024 07:00:00 GMT</pubDate>
</item>
<item>
  <title>Looking for the log-multiplication trick inside a small language model</title>
  <dc:creator>Huangxing Li</dc:creator>
  <link>https://hli1234.github.io/probing-small-lms/posts/log_shortcut_findings/</link>
  <description><![CDATA[ 





<section id="the-question" class="level2">
<h2 class="anchored" data-anchor-id="the-question">The question</h2>
<p>For three centuries before the pocket calculator, engineers multiplied with slide rules. The slide rule is a physical implementation of one identity:</p>
<p><span id="eq-shortcut"><img src="https://latex.codecogs.com/png.latex?%0Aa%20%5Ccdot%20b%20=%202%5E%7B%5Clog_2%20a%20+%20%5Clog_2%20b%7D%0A%5Ctag%7B1%7D"></span></p>
<p>Three lookups and an addition, instead of a multiplication. The trick rests on a different <em>geometry</em> of number — positions on the slide rule are spaced by <img src="https://latex.codecogs.com/png.latex?%5Clog_2">, not by raw magnitude, so adding two distances corresponds to multiplying the underlying values.</p>
<p>Does a neural network ever discover this geometry? If you train a model to predict text and it sees enough numbers, does it secretly learn the slide rule?</p>
<p>There’s a longer plan in <a href="experiment1.md"><code>experiment1.md</code></a> to train a fresh tiny Transformer from scratch under capacity pressure and dissect it — Anthropic-style mechanistic interpretability. That’s weeks of work. This post is the parallel and cheaper question: <strong>is the shortcut already sitting inside an existing small language model?</strong></p>
<p>To find out, I poked at GPT-2 small (the 124M-parameter model from 2019) and Pythia-160M (a more modern but similarly-sized model) with two tools: linear probes (a regression that reads a value off the residual stream) and activation patching (an intervention that surgically changes one direction in the residual stream and watches what happens downstream).</p>
<div class="callout callout-style-default callout-note callout-titled" title="Summary in one figure">
<div class="callout-header d-flex align-content-center">
<div class="callout-icon-container">
<i class="callout-icon"></i>
</div>
<div class="callout-title-container flex-fill">
<span class="screen-reader-only">Note</span>Summary in one figure
</div>
</div>
<div class="callout-body-container callout-body">
<div id="fig-summary" class="quarto-float quarto-figure quarto-figure-center anchored">
<figure class="quarto-float quarto-float-fig figure">
<div aria-describedby="fig-summary-caption-0ceaefa1-69ba-4598-a22c-09a6ac19f8ca">
<img src="https://hli1234.github.io/probing-small-lms/posts/log_shortcut_findings/summary_figure.png" class="img-fluid figure-img">
</div>
<figcaption class="quarto-float-caption-bottom quarto-float-caption quarto-float-fig" id="fig-summary-caption-0ceaefa1-69ba-4598-a22c-09a6ac19f8ca">
Figure&nbsp;1: Six panels covering three models (GPT-2 small / Pythia-70M / Pythia-160M). <strong>A:</strong> Embedding log-axis decodability in all three. Log dominates only under weight tying; untied Pythia models prefer linear by a hair. <strong>B:</strong> GPT-2 <em>does</em> compute <img src="https://latex.codecogs.com/png.latex?%5Clog_2(a%20%5Ccdot%20b)"> at the equals-sign at <img src="https://latex.codecogs.com/png.latex?R%5E2%20=%200.989"> — but on a different axis than the one used to read the operands. <strong>C:</strong> New result. Patching along that fresh answer-side direction moves the LM head’s argmax with slope only <img src="https://latex.codecogs.com/png.latex?0.07"> — the probe is correlational, not causal. <strong>D:</strong> LM-head log-axis fraction collapses to baseline once weights are untied, so GPT-2’s “28× baseline” was mostly architectural. <strong>E:</strong> Near-miss slope <img src="https://latex.codecogs.com/png.latex?%5Cbeta"> is invariant at ~0.22 across all three models, even as panels A and D vary by orders of magnitude. <strong>F:</strong> synthesis.
</figcaption>
</figure>
</div>
</div>
</div>
</section>
<section id="a-simple-model-first-when-should-a-learner-discover-the-shortcut" class="level2">
<h2 class="anchored" data-anchor-id="a-simple-model-first-when-should-a-learner-discover-the-shortcut">A simple model first: when <em>should</em> a learner discover the shortcut?</h2>
<p>Before touching real models I built a toy that makes the cost-pressure hypothesis quantitative. Two paths can compute <img src="https://latex.codecogs.com/png.latex?z%20=%20a%20%5Ccdot%20b">:</p>
<ul>
<li><strong>Direct multiplication</strong>, with cost <img src="https://latex.codecogs.com/png.latex?c_%7B%5Cmathrm%7Bmul%7D%7D(a,%20b)%20=%20%5Clog_2(a)%20%5Ccdot%20%5Clog_2(b)"> — bit × bit schoolbook work. <img src="https://latex.codecogs.com/png.latex?2%20%5Ctimes%202"> is one bit times one bit; <img src="https://latex.codecogs.com/png.latex?1024%20%5Ctimes%20512"> is ten bits times nine.</li>
<li><strong>The log–sum–exp shortcut</strong>, with cost <img src="https://latex.codecogs.com/png.latex?c_%7B%5Cmathrm%7Bshort%7D%7D%20=%206"> — three primitive operations, independent of operand size.</li>
</ul>
<p>A learnable gate <img src="https://latex.codecogs.com/png.latex?p%20%5Cin%20%5B0,%201%5D"> reads <img src="https://latex.codecogs.com/png.latex?(%5Clog_2%20a,%20%5Clog_2%20b)"> and picks how much shortcut to use. The expected total cost at penalty weight <img src="https://latex.codecogs.com/png.latex?%5Clambda"> is</p>
<p><span id="eq-cost"><img src="https://latex.codecogs.com/png.latex?%0AC(p,%20a,%20b,%20%5Clambda)%20=%20(1-p)%5Ccdot%20%5Clambda%20%5Ccdot%20c_%7B%5Cmathrm%7Bmul%7D%7D(a,%20b)%20+%20p%20%5Ccdot%20c_%7B%5Cmathrm%7Bshort%7D%7D.%0A%5Ctag%7B2%7D"></span></p>
<p>Sweep <img src="https://latex.codecogs.com/png.latex?%5Clambda">. The aggregate probability of taking the shortcut traces a smooth sigmoid. The interesting geometry is in which <img src="https://latex.codecogs.com/png.latex?(a,%20b)"> pairs switch first — hard ones do, easy ones hold out, and trivial ones (those involving <img src="https://latex.codecogs.com/png.latex?1">) never switch, because <img src="https://latex.codecogs.com/png.latex?%5Clog_2(1)%20=%200"> makes direct multiplication genuinely free.</p>
<div id="fig-toy-routing" class="quarto-float quarto-figure quarto-figure-center anchored">
<figure class="quarto-float quarto-float-fig figure">
<div aria-describedby="fig-toy-routing-caption-0ceaefa1-69ba-4598-a22c-09a6ac19f8ca">
<img src="https://hli1234.github.io/probing-small-lms/posts/log_shortcut_findings/reasoning_path_vs_penalty.png" class="img-fluid figure-img">
</div>
<figcaption class="quarto-float-caption-bottom quarto-float-caption quarto-float-fig" id="fig-toy-routing-caption-0ceaefa1-69ba-4598-a22c-09a6ac19f8ca">
Figure&nbsp;2: Toy soft-routing model under varying multiplication-penalty weight <img src="https://latex.codecogs.com/png.latex?%5Clambda">. <strong>Top:</strong> average shortcut probability across <img src="https://latex.codecogs.com/png.latex?(a,%20b)%20%5Cin%20%5B1,%2064%5D%5E2">. The trained gate matches the analytical softmin to three decimal places. <strong>Bottom:</strong> per-pair gate at five snapshot <img src="https://latex.codecogs.com/png.latex?%5Clambda"> values; the decision boundary sweeps from upper-right (hard pairs) to lower-left (easy pairs) as the penalty grows.
</figcaption>
</figure>
</div>
<p>This isn’t itself a finding. It’s a yardstick — the routing geometry predicted by the simplest possible cost-cutter. The empirical question is whether real language models trained on text, not on multiplication, show even a faint trace of this geometry.</p>
</section>
<section id="how-gpt-2-represents-numbers" class="level2">
<h2 class="anchored" data-anchor-id="how-gpt-2-represents-numbers">How GPT-2 represents numbers</h2>
<p>To check what a model has internalised about integers, take the residual stream — the model’s internal scratchpad at each layer — at the position of a number token, and ask: can a linear regression predict <img src="https://latex.codecogs.com/png.latex?%5Clog_2(x)"> from it? If yes, the model has a “log of this number” axis at that layer.</p>
<p>I fed GPT-2 small the integers 2 through 1023, each as <code>" 5"</code>, <code>" 17"</code>, <code>" 1024"</code>, etc. Most are single BPE tokens. At every layer I extracted the residual stream and trained a ridge probe for <img src="https://latex.codecogs.com/png.latex?%5Clog_2(x)"> and several controls.</p>
<p><strong>At the very first layer — the raw token embedding, before any transformer block has run — <img src="https://latex.codecogs.com/png.latex?%5Clog_2(x)"> is decodable at <img src="https://latex.codecogs.com/png.latex?R%5E2%20=%200.95"> on held-out integers.</strong> Raw <img src="https://latex.codecogs.com/png.latex?x"> is also decodable, but worse, at <img src="https://latex.codecogs.com/png.latex?R%5E2%20=%200.77">. Shuffled labels (a noise control) give <img src="https://latex.codecogs.com/png.latex?R%5E2%20%5Capprox%200">. So the embedding prefers the log shape.</p>
<p>To confirm the axis is really log-shaped and not just “any monotone function of <img src="https://latex.codecogs.com/png.latex?x">”, I swept the power-law family <img src="https://latex.codecogs.com/png.latex?x%5E%5Calpha">. Decodability peaks right around <img src="https://latex.codecogs.com/png.latex?%5Calpha%20=%200"> — the log limit. If GPT-2 stored linear magnitude the peak would sit at <img src="https://latex.codecogs.com/png.latex?%5Calpha%20=%201">. It doesn’t.</p>
<p>What’s more surprising is what <em>else</em> the embedding stores. Parity is decodable at <img src="https://latex.codecogs.com/png.latex?R%5E2%20=%200.94">. Last digit (<img src="https://latex.codecogs.com/png.latex?x%20%5Cbmod%2010">) at <img src="https://latex.codecogs.com/png.latex?R%5E2%20=%200.92">. GPT-2 has noticed, just from reading text, that <code>" 5"</code>, <code>" 15"</code>, <code>" 25"</code> all share the feature of ending in <code>5</code>. Ask the same probe for <code>"is x divisible by 7?"</code> and the <img src="https://latex.codecogs.com/png.latex?R%5E2"> is essentially zero. Same for <code>"is x prime?"</code>. The embedding has structure for the things a careful reader of natural language might track — size, parity, last digit — and ignores number-theory properties that show up too rarely in text to matter.</p>
<div id="fig-controls" class="quarto-float quarto-figure quarto-figure-center anchored">
<figure class="quarto-float quarto-float-fig figure">
<div aria-describedby="fig-controls-caption-0ceaefa1-69ba-4598-a22c-09a6ac19f8ca">
<img src="https://hli1234.github.io/probing-small-lms/posts/log_shortcut_findings/probe_gpt2_controls.png" class="img-fluid figure-img">
</div>
<figcaption class="quarto-float-caption-bottom quarto-float-caption quarto-float-fig" id="fig-controls-caption-0ceaefa1-69ba-4598-a22c-09a6ac19f8ca">
Figure&nbsp;3: Linear-probe <img src="https://latex.codecogs.com/png.latex?R%5E2"> at each layer for monotone targets, non-monotone targets, and a noise baseline. The log axis, parity, and last digit are all decodable at the embedding (<img src="https://latex.codecogs.com/png.latex?R%5E2%20%3E%200.9">). Number-theoretic properties (mod 7, primality) and the shuffle control are at chance.
</figcaption>
</figure>
</div>
<section id="does-this-generalise-across-categories-of-numbers" class="level3">
<h3 class="anchored" data-anchor-id="does-this-generalise-across-categories-of-numbers">Does this generalise across categories of numbers?</h3>
<p>A natural question: does the integer log axis carry over to other numeric tokens — years like <code>" 1990"</code>, round numbers like <code>" 1000"</code>, number-words like <code>" million"</code>? A simple model says yes: GPT-2 has one shared magnitude axis for everything numeric.</p>
<p>The data says no, decisively. Apply the integer-trained probe to:</p>
<ul>
<li>152 single-token years from <img src="https://latex.codecogs.com/png.latex?%5B1500,%202099%5D"> → <img src="https://latex.codecogs.com/png.latex?R%5E2%20=%20-2274">. The readouts are nearly constant regardless of which year you feed in. The integer axis can’t distinguish 1500 from 2099 at all.</li>
<li>33 single-token round numbers (multiples of 100 up to 100 000) → <img src="https://latex.codecogs.com/png.latex?R%5E2%20=%200.22">. Weak positive transfer.</li>
<li>24 single-token English number-words (“one”…“twenty”, “hundred”, “thousand”, “million”, “billion”) → <img src="https://latex.codecogs.com/png.latex?R%5E2%20=%200.14">.</li>
</ul>
<div id="fig-shared-axis" class="quarto-float quarto-figure quarto-figure-center anchored">
<figure class="quarto-float quarto-float-fig figure">
<div aria-describedby="fig-shared-axis-caption-0ceaefa1-69ba-4598-a22c-09a6ac19f8ca">
<img src="https://hli1234.github.io/probing-small-lms/posts/log_shortcut_findings/probe_gpt2_shared_axis.png" class="img-fluid figure-img">
</div>
<figcaption class="quarto-float-caption-bottom quarto-float-caption quarto-float-fig" id="fig-shared-axis-caption-0ceaefa1-69ba-4598-a22c-09a6ac19f8ca">
Figure&nbsp;4: The integer-trained log probe applied to other numeric categories. Integers (training) sit cleanly on <img src="https://latex.codecogs.com/png.latex?y%20=%20x">. Years, round numbers, and number-words sit far off the line. Each numeric category lives on its own axis in embedding space; there is no universal magnitude direction.
</figcaption>
</figure>
</div>
<p>This tells you something about how GPT-2 represents numbers. The token <code>" 1990"</code> is encoded primarily as “a year” — its role in text — and only weakly as “the number 1990”. The model’s representation is role-dependent, not value-dependent. A probe trained on integers reads years as nonsense.</p>
</section>
</section>
<section id="does-the-log-axis-actually-do-anything" class="level2">
<h2 class="anchored" data-anchor-id="does-the-log-axis-actually-do-anything">Does the log axis actually do anything?</h2>
<p>Decodability is a correlational fact. To test whether the model <em>uses</em> the log axis when it multiplies, I ran activation patching at the operand token.</p>
<p>Take a prompt like <code>" 5 * 9 ="</code>. The model processes it normally. At the position where <code>" 5"</code> sits, the embedding’s projection onto the probe direction <img src="https://latex.codecogs.com/png.latex?w"> reads <img src="https://latex.codecogs.com/png.latex?%5Clog_2(5)%20%5Capprox%202.3">. I add a rank-1 vector along <img src="https://latex.codecogs.com/png.latex?w"> so that projection now reads <img src="https://latex.codecogs.com/png.latex?%5Clog_2(2)%20=%201">. Everything else about the embedding stays the same — same parity, same last-digit, same noise. Then I let the model run the full 12 layers and ask: which numeric token does the next-token distribution prefer?</p>
<p>For <code>" 5 * 9 ="</code>, patching <img src="https://latex.codecogs.com/png.latex?%5Clog_2(5)%20%5Cto%20%5Clog_2(2)"> makes the logit for <code>" 18"</code> (= <img src="https://latex.codecogs.com/png.latex?2%20%5Ccdot%209">) rise <em>above</em> the logit for <code>" 45"</code> (= <img src="https://latex.codecogs.com/png.latex?5%20%5Ccdot%209">). The model has been talked into preferring the patched product. Patching toward <img src="https://latex.codecogs.com/png.latex?4"> makes <code>" 36"</code> win. Patching toward <img src="https://latex.codecogs.com/png.latex?5"> does nothing, as it should. And — the critical control — patching along five different random directions of equal magnitude to <img src="https://latex.codecogs.com/png.latex?w"> gives logit shifts indistinguishable from zero. The effect is specific to the probe direction.</p>
<p>The effect is also <em>asymmetric</em>. Patching downward (toward smaller numbers) works cleanly. Patching upward (toward larger numbers) only partly works. Most plausible explanation: the LM head has a strong frequency prior favouring small, common numbers in text. A fixed-size patch is enough to push preference toward smaller numbers (with the prior) but not enough to push it toward larger numbers (against the prior).</p>
<div id="fig-patching" class="quarto-float quarto-figure quarto-figure-center anchored">
<figure class="quarto-float quarto-float-fig figure">
<div aria-describedby="fig-patching-caption-0ceaefa1-69ba-4598-a22c-09a6ac19f8ca">
<img src="https://hli1234.github.io/probing-small-lms/posts/log_shortcut_findings/probe_gpt2_patching.png" class="img-fluid figure-img">
</div>
<figcaption class="quarto-float-caption-bottom quarto-float-caption quarto-float-fig" id="fig-patching-caption-0ceaefa1-69ba-4598-a22c-09a6ac19f8ca">
Figure&nbsp;5: Causal patching at the operand position. <strong>Left:</strong> change in relative logit, <img src="https://latex.codecogs.com/png.latex?%5Cmathrm%7Blogit%7D(a_%7B%5Cmathrm%7Btarget%7D%7D%20%5Ccdot%20b)%20-%20%5Cmathrm%7Blogit%7D(a%20%5Ccdot%20b)">, as we patch the log direction at the <code>" a"</code> position. Solid lines are the log direction; dotted lines are random directions of the same norm. <strong>Right:</strong> heatmap of relative logits for <code>" 5 * 9 ="</code>. The yellow most-favoured-product stripe follows the perfect-log-shortcut line <img src="https://latex.codecogs.com/png.latex?p%20=%20a_%7B%5Cmathrm%7Btarget%7D%7D%20%5Ccdot%20b"> for downward patches, then sticks at 45 once we patch toward larger numbers.
</figcaption>
</figure>
</div>
</section>
<section id="does-the-model-actually-compute-logab" class="level2">
<h2 class="anchored" data-anchor-id="does-the-model-actually-compute-logab">Does the model actually compute log(a·b)?</h2>
<p>This is the test I most wanted to run.</p>
<p>If the model does the slide-rule trick — adds the operand logs and then exponentiates at the end — then at the position of the <code>=</code> sign, the residual stream right before the LM head should be carrying <img src="https://latex.codecogs.com/png.latex?%5Clog_2(a%20%5Ccdot%20b)">. The operand-side log direction <img src="https://latex.codecogs.com/png.latex?w"> ought to read off this value.</p>
<p>I checked. For <code>" 5 * 9 ="</code>, the operand-side probe at <code>=</code> reads off <strong>10.76</strong> — not <img src="https://latex.codecogs.com/png.latex?%5Clog_2(45)%20%5Capprox%205.49">. For <code>" 3 * 7 ="</code> it reads <strong>11.17</strong>, not <img src="https://latex.codecogs.com/png.latex?%5Clog_2(21)%20%5Capprox%204.39">. Across nine different prompts, the readout is essentially flat. The <img src="https://latex.codecogs.com/png.latex?R%5E2"> of “what the operand-axis probe reads at <code>=</code>” against “the true <img src="https://latex.codecogs.com/png.latex?%5Clog_2(a%20%5Ccdot%20b)">” is <strong>0.011</strong> — no signal.</p>
<p>My first reaction was that the circuit isn’t there. The shortcut exists on the input side, but the model isn’t wiring it together.</p>
<p>Then it occurred to me: maybe the model uses a <em>different</em> direction at the <code>=</code> position to write down the answer’s log than the one it uses to read the operands’ logs. There’s no architectural reason those have to be the same vector — the model can translate between representations as it computes. So I trained a fresh ridge probe directly on the residual at <code>=</code>, against <img src="https://latex.codecogs.com/png.latex?%5Clog_2(a%20%5Ccdot%20b)"> for several hundred random <img src="https://latex.codecogs.com/png.latex?(a,%20b)"> pairs.</p>
<div class="callout callout-style-default callout-important callout-titled" title="The key result">
<div class="callout-header d-flex align-content-center">
<div class="callout-icon-container">
<i class="callout-icon"></i>
</div>
<div class="callout-title-container flex-fill">
<span class="screen-reader-only">Important</span>The key result
</div>
</div>
<div class="callout-body-container callout-body">
<p>The new probe predicts the answer’s log at <strong><img src="https://latex.codecogs.com/png.latex?R%5E2%20=%200.989"></strong> on held-out <img src="https://latex.codecogs.com/png.latex?(a,%20b)"> pairs. The model has computed <img src="https://latex.codecogs.com/png.latex?%5Clog_2(a%20%5Ccdot%20b)">. It’s there. It’s almost perfectly linearly decodable from the residual stream right before the LM head. I had simply been looking on the wrong axis.</p>
</div>
</div>
<p>So the picture is this. The operands’ logs are written down on one axis at the input. Somewhere in the middle layers, the model adds them and rewrites the result on a <em>different</em> axis at the <code>=</code> position. The LM head then reads that second axis to decide what number comes next. The whole <img src="https://latex.codecogs.com/png.latex?%5Clog%20%5Cto%20%5Cmathrm%7Bsum%7D%20%5Cto%20%5Cexp"> machine is sitting in GPT-2 small.</p>
<div id="fig-pipeline" class="quarto-float quarto-figure quarto-figure-center anchored">
<figure class="quarto-float quarto-float-fig figure">
<div aria-describedby="fig-pipeline-caption-0ceaefa1-69ba-4598-a22c-09a6ac19f8ca">
<img src="https://hli1234.github.io/probing-small-lms/posts/log_shortcut_findings/probe_gpt2_pipeline.png" class="img-fluid figure-img">
</div>
<figcaption class="quarto-float-caption-bottom quarto-float-caption quarto-float-fig" id="fig-pipeline-caption-0ceaefa1-69ba-4598-a22c-09a6ac19f8ca">
Figure&nbsp;6: The end-to-end pipeline. <strong>Left:</strong> patching the log component at the <code>=</code> position of real prompts; argmax over numeric tokens is stuck at <code>" 1"</code> regardless of <img src="https://latex.codecogs.com/png.latex?v_%7B%5Cmathrm%7Btarget%7D%7D">. <strong>Middle:</strong> the operand-side probe’s readout at <code>=</code> is roughly constant (<img src="https://latex.codecogs.com/png.latex?%5Csim%2010.8">) across all prompts, uncorrelated with <img src="https://latex.codecogs.com/png.latex?%5Clog_2(a%20%5Ccdot%20b)"> (<img src="https://latex.codecogs.com/png.latex?R%5E2%20=%200.011">). <strong>Right:</strong> a fresh probe trained directly on residuals at <code>=</code> predicts <img src="https://latex.codecogs.com/png.latex?%5Clog_2(a%20%5Ccdot%20b)"> at <img src="https://latex.codecogs.com/png.latex?R%5E2%20=%200.989"> on held-out pairs.
</figcaption>
</figure>
</div>
<section id="a-correction-the-answer-side-direction-is-correlational-not-causal" class="level3">
<h3 class="anchored" data-anchor-id="a-correction-the-answer-side-direction-is-correlational-not-causal">A correction: the answer-side direction is correlational, not causal</h3>
<p>The <img src="https://latex.codecogs.com/png.latex?R%5E2%20=%200.989"> number is striking enough that I should ask whether the direction the probe found is actually the one the model <em>uses</em>, rather than some downstream feature that <em>correlates</em> with <img src="https://latex.codecogs.com/png.latex?%5Clog_2(a%20%5Ccdot%20b)">. That’s the classic correlation-vs-causation issue with linear probes, sometimes called the “probing illusion”.</p>
<p>The clean way to test causation: take the fitted answer-side direction <img src="https://latex.codecogs.com/png.latex?w_%7B%5Cmathrm%7Bans%7D%7D">, then for various <img src="https://latex.codecogs.com/png.latex?v_%7B%5Cmathrm%7Btarget%7D%7D">, patch the residual at <code>=</code> so the projection onto <img src="https://latex.codecogs.com/png.latex?w_%7B%5Cmathrm%7Bans%7D%7D"> equals <img src="https://latex.codecogs.com/png.latex?v_%7B%5Cmathrm%7Btarget%7D%7D">, apply the LM head, and check whether the argmax over numeric tokens tracks <img src="https://latex.codecogs.com/png.latex?2%5E%7Bv_%7B%5Cmathrm%7Btarget%7D%7D%7D">.</p>
<p>If the model uses this direction, the LM head’s argmax should follow <img src="https://latex.codecogs.com/png.latex?2%5E%7Bv_%7B%5Cmathrm%7Btarget%7D%7D%7D"> — slope 1 on a log-log plot. If it merely <em>correlates</em> with <img src="https://latex.codecogs.com/png.latex?%5Clog_2(a%20%5Ccdot%20b)">, patching it should move nothing.</p>
<p>The result: <strong>slope <img src="https://latex.codecogs.com/png.latex?0.072">, not 1</strong>. For most prompts the argmax over numeric tokens doesn’t change at all across an 11-log-unit sweep. The LM head clearly doesn’t read this direction. The 0.989 R² says <img src="https://latex.codecogs.com/png.latex?%5Clog_2(a%20%5Ccdot%20b)"> is <em>encoded</em> there — but probably as a downstream consequence of whatever the model is actually doing, not because the LM head reads it.</p>
<div class="callout callout-style-default callout-warning callout-titled" title="Honest revision">
<div class="callout-header d-flex align-content-center">
<div class="callout-icon-container">
<i class="callout-icon"></i>
</div>
<div class="callout-title-container flex-fill">
<span class="screen-reader-only">Warning</span>Honest revision
</div>
</div>
<div class="callout-body-container callout-body">
<p>The original write-up of this experiment claimed the pipeline was “wired up” because the answer’s log was decodable at <img src="https://latex.codecogs.com/png.latex?R%5E2%20=%200.989">. The causal test shows that’s overclaiming. The decodability is real, but the LM head doesn’t act on that specific direction. The right summary is: <strong>the model has computed something equivalent to <img src="https://latex.codecogs.com/png.latex?%5Clog_2(a%20%5Ccdot%20b)"> somewhere in its residual stream at <code>=</code>, but the LM head’s actual choice of next token is driven by other directions.</strong> What those directions encode, and how they relate to the operand-side log axis, is still open.</p>
</div>
</div>
<div id="fig-answer-side-causal" class="quarto-float quarto-figure quarto-figure-center anchored">
<figure class="quarto-float quarto-float-fig figure">
<div aria-describedby="fig-answer-side-causal-caption-0ceaefa1-69ba-4598-a22c-09a6ac19f8ca">
<img src="https://hli1234.github.io/probing-small-lms/posts/log_shortcut_findings/probe_gpt2_answer_side_causal.png" class="img-fluid figure-img">
</div>
<figcaption class="quarto-float-caption-bottom quarto-float-caption quarto-float-fig" id="fig-answer-side-causal-caption-0ceaefa1-69ba-4598-a22c-09a6ac19f8ca">
Figure&nbsp;7: Patching along the fitted answer-side direction <img src="https://latex.codecogs.com/png.latex?w_%7B%5Cmathrm%7Bans%7D%7D"> at the <code>=</code> position. <strong>Left:</strong> for each of eight multiplication prompts, <img src="https://latex.codecogs.com/png.latex?%5Clog_2"> of the LM head’s argmax-numeric vs the injected <img src="https://latex.codecogs.com/png.latex?v_%7B%5Cmathrm%7Btarget%7D%7D">. Compared to the dashed reference <img src="https://latex.codecogs.com/png.latex?y%20=%20v_%7B%5Cmathrm%7Btarget%7D%7D">, the curves are essentially flat — the LM head doesn’t read this direction. <strong>Right:</strong> rank of the closest single-token integer to <img src="https://latex.codecogs.com/png.latex?2%5E%7Bv_%7B%5Cmathrm%7Btarget%7D%7D%7D"> among all numeric tokens. The target never ends up at rank 0; it bottoms around 100-300 out of 638 numeric tokens.
</figcaption>
</figure>
</div>
</section>
</section>
<section id="two-more-checks-was-the-other-finding-real-or-an-artifact" class="level2">
<h2 class="anchored" data-anchor-id="two-more-checks-was-the-other-finding-real-or-an-artifact">Two more checks: was the other finding real or an artifact?</h2>
<p>The <img src="https://latex.codecogs.com/png.latex?0.989"> number is striking enough that I want to ask twice. Two follow-ups, both quantitative.</p>
<section id="check-1-does-the-lm-head-act-on-the-log-axis" class="level3">
<h3 class="anchored" data-anchor-id="check-1-does-the-lm-head-act-on-the-log-axis">Check 1 — does the LM head act on the log axis?</h3>
<p>The LM head’s row for each numeric token is what gets dotted with the residual stream to produce the next-token logit. If the log axis matters for output, those rows should have non-trivial mass on it.</p>
<p>For GPT-2 small: the log direction captures 3.6% of each numeric LM-head row’s squared norm on average, versus <img src="https://latex.codecogs.com/png.latex?1/d%20=%200.13%5C%25"> for a random direction — about <img src="https://latex.codecogs.com/png.latex?28%5Ctimes"> the isotropic baseline.</p>
<p>But there’s a catch. GPT-2 ties its LM head to its embedding, so the LM head row for token <code>" 5"</code> is <em>literally</em> the same vector as the embedding of <code>" 5"</code>. The “alignment” of LM head with log axis is automatic, not emergent. To separate “tying” from “emergent alignment”, I re-ran the analysis on two more models with untied weights — <strong>Pythia-70M</strong> and <strong>Pythia-160M</strong>:</p>
<table class="caption-top table">
<colgroup>
<col style="width: 8%">
<col style="width: 13%">
<col style="width: 8%">
<col style="width: 33%">
<col style="width: 35%">
</colgroup>
<thead>
<tr class="header">
<th>Model</th>
<th>Hidden <img src="https://latex.codecogs.com/png.latex?d"></th>
<th>Tied?</th>
<th>LM-head log-axis fraction</th>
<th>Multiple of <img src="https://latex.codecogs.com/png.latex?1/d"> baseline</th>
</tr>
</thead>
<tbody>
<tr class="odd">
<td>GPT-2 small (124M)</td>
<td>768</td>
<td>tied</td>
<td>3.6%</td>
<td><strong>28×</strong></td>
</tr>
<tr class="even">
<td>Pythia-70M</td>
<td>512</td>
<td>untied</td>
<td>0.19%</td>
<td><strong>1.0×</strong></td>
</tr>
<tr class="odd">
<td>Pythia-160M</td>
<td>768</td>
<td>untied</td>
<td>0.01%</td>
<td><strong>0.1×</strong></td>
</tr>
</tbody>
</table>
<p>Without weight tying, the LM head doesn’t independently align with the integer-embedding log axis. The underlying log axis is real (decodable at <img src="https://latex.codecogs.com/png.latex?R%5E2%20%5Capprox%200.89"> in both Pythia embeddings too — see panel A of Figure&nbsp;1), but the LM head doesn’t inherit it for free when weights are untied. The “28× baseline” number for GPT-2 was largely an artifact of weight tying.</p>
<div id="fig-lmhead" class="quarto-float quarto-figure quarto-figure-center anchored">
<figure class="quarto-float quarto-float-fig figure">
<div aria-describedby="fig-lmhead-caption-0ceaefa1-69ba-4598-a22c-09a6ac19f8ca">
<img src="https://hli1234.github.io/probing-small-lms/posts/log_shortcut_findings/probe_gpt2_lmhead.png" class="img-fluid figure-img">
</div>
<figcaption class="quarto-float-caption-bottom quarto-float-caption quarto-float-fig" id="fig-lmhead-caption-0ceaefa1-69ba-4598-a22c-09a6ac19f8ca">
Figure&nbsp;8: LM-head log-axis decomposition for GPT-2 small. The 3.6% number averaged over numeric tokens is real, but the comparison with Pythia (see panel C of Figure&nbsp;1) shows most of that comes from weight tying.
</figcaption>
</figure>
</div>
</section>
<section id="check-2-does-the-log-axis-pull-on-the-actual-output-distribution" class="level3">
<h3 class="anchored" data-anchor-id="check-2-does-the-log-axis-pull-on-the-actual-output-distribution">Check 2 — does the log axis pull on the actual output distribution?</h3>
<p>The behavioural test. For 200 random multiplication prompts <img src="https://latex.codecogs.com/png.latex?(a,%20b)%20%5Cin%20%5B2,%2050)%5E2">, I look at the model’s next-token distribution restricted to numeric tokens, and fit a simple log-Gaussian:</p>
<p><span id="eq-gauss"><img src="https://latex.codecogs.com/png.latex?%0AP(%5Ctext%7Bpredict%20%7D%20n%20%5Cmid%20a,%20b)%20%5Cpropto%20%5Cexp%5C!%5Cleft(-%5Cfrac%7B(%5Clog_2%20n%20-%20%5Cmu)%5E2%7D%7B2%5Csigma%5E2%7D%5Cright).%0A%5Ctag%7B3%7D"></span></p>
<p>If the log circuit drives output, <img src="https://latex.codecogs.com/png.latex?%5Cmu%20%5Capprox%20%5Clog_2(a%20%5Ccdot%20b)"> and <img src="https://latex.codecogs.com/png.latex?%5Csigma"> shrinks with confidence. If the log axis is ignored, <img src="https://latex.codecogs.com/png.latex?%5Cmu"> has no relation to the true log.</p>
<p>The result sits between these. Across 200 prompts:</p>
<ul>
<li><img src="https://latex.codecogs.com/png.latex?0/200"> argmaxes are correct (GPT-2 small really can’t multiply).</li>
<li>Fitted <img src="https://latex.codecogs.com/png.latex?%5Cmu"> is biased <img src="https://latex.codecogs.com/png.latex?%5Cmathbf%7B-3.25%7D"> log-units <em>below</em> the true <img src="https://latex.codecogs.com/png.latex?%5Clog_2(a%20%5Ccdot%20b)"> on average. The model predicts numbers about <img src="https://latex.codecogs.com/png.latex?2%5E%7B3.25%7D%20%5Capprox%209.5%5Ctimes"> smaller than the right answer.</li>
<li>But the slope of <img src="https://latex.codecogs.com/png.latex?%5Cmu"> vs <img src="https://latex.codecogs.com/png.latex?%5Clog_2(a%20%5Ccdot%20b)"> across prompts is <img src="https://latex.codecogs.com/png.latex?%5Cboldsymbol%7B%5Cbeta%20=%200.22%7D">. Not 1 (perfect readout), not 0 (no readout), but a clear positive coupling.</li>
</ul>
<p>Repeating the experiment on Pythia-70M gives <img src="https://latex.codecogs.com/png.latex?%5Cbeta%20=%200.22"> — same as GPT-2. On Pythia-160M I get <img src="https://latex.codecogs.com/png.latex?%5Cbeta%20=%200.19">. Across three models with very different LM-head geometries (4 orders of magnitude apart in their log-axis fraction), <strong>the behavioural pull on output is the same</strong>: about 22% of what a clean readout would produce. The slope <img src="https://latex.codecogs.com/png.latex?%5Cbeta%20%5Capprox%0A0.22"> looks like a property of the cost-sensitive output distribution the models share, not of how their LM heads happen to align with integer-embedding directions.</p>
<table class="caption-top table">
<thead>
<tr class="header">
<th>Model</th>
<th>LM-head log-axis fraction</th>
<th>Near-miss <img src="https://latex.codecogs.com/png.latex?%5Cbeta"></th>
</tr>
</thead>
<tbody>
<tr class="odd">
<td>GPT-2 small</td>
<td>3.6% (28× baseline)</td>
<td>0.22</td>
</tr>
<tr class="even">
<td>Pythia-70M</td>
<td>0.19% (1.0× baseline)</td>
<td>0.22</td>
</tr>
<tr class="odd">
<td>Pythia-160M</td>
<td>0.01% (0.1× baseline)</td>
<td>0.19</td>
</tr>
</tbody>
</table>
<div id="fig-near-miss" class="quarto-float quarto-figure quarto-figure-center anchored">
<figure class="quarto-float quarto-float-fig figure">
<div aria-describedby="fig-near-miss-caption-0ceaefa1-69ba-4598-a22c-09a6ac19f8ca">
<img src="https://hli1234.github.io/probing-small-lms/posts/log_shortcut_findings/probe_gpt2_near_miss.png" class="img-fluid figure-img">
</div>
<figcaption class="quarto-float-caption-bottom quarto-float-caption quarto-float-fig" id="fig-near-miss-caption-0ceaefa1-69ba-4598-a22c-09a6ac19f8ca">
Figure&nbsp;9: Near-miss arithmetic for GPT-2. <strong>Left:</strong> fitted mean log of the predicted distribution vs true <img src="https://latex.codecogs.com/png.latex?%5Clog_2(a%20%5Ccdot%20b)">. The mean predicted log tracks the true log with slope <img src="https://latex.codecogs.com/png.latex?%5Cbeta%20=%200.22">, biased about 3 units below the <img src="https://latex.codecogs.com/png.latex?y%20=%20x"> line. <strong>Middle:</strong> fitted spread <img src="https://latex.codecogs.com/png.latex?%5Csigma"> is roughly constant at <img src="https://latex.codecogs.com/png.latex?%5Csim%201.9"> log-units. <strong>Right:</strong> three example predicted distributions; dotted vertical lines mark the true <img src="https://latex.codecogs.com/png.latex?%5Clog_2(a%20%5Ccdot%20b)">.
</figcaption>
</figure>
</div>
<p>This <img src="https://latex.codecogs.com/png.latex?%5Cbeta%20=%200.22"> ties everything together. The log circuit puts <img src="https://latex.codecogs.com/png.latex?%5Clog_2(a%20%5Ccdot%20b)"> in the residual stream at <img src="https://latex.codecogs.com/png.latex?R%5E2%20=%200.989">. The LM head acts on it to roughly 22% of what perfect readout would give, swamped by a much larger frequency prior. The machine is wired up; it just isn’t loud enough.</p>
</section>
</section>
<section id="what-this-collectively-shows" class="level2">
<h2 class="anchored" data-anchor-id="what-this-collectively-shows">What this collectively shows</h2>
<p>Five claims, the first three robust and the last two more tentative.</p>
<p>The embedding layer of all three models encodes integers on an approximately logarithmic axis, along with separate axes for parity and last digit. Number-theoretic properties not visible in surface text (mod 7, primality) do not get a coherent representation. The log-axis shape peaks the power-law sweep at <img src="https://latex.codecogs.com/png.latex?%5Calpha%20=%200">, but the absolute preference of log over linear is partly weight-tying dependent — GPT-2 has log <img src="https://latex.codecogs.com/png.latex?%5Cgg"> linear at the embedding, while untied Pythia models have log <img src="https://latex.codecogs.com/png.latex?%5Capprox"> linear there.</p>
<p>Perturbations along the embedding’s log axis have structured, direction-specific causal effect on the model’s next-token predictions in multiplication prompts. Random directions of the same magnitude do not. This rules out the “probe is finding a noise direction” null hypothesis. The asymmetry of the effect (downward patches work, upward patches partly fail) points at the LM head’s frequency prior as the limiting factor.</p>
<p>In all three models, the behavioural pull of the log axis on actual output is invariant at <img src="https://latex.codecogs.com/png.latex?%5Cbeta%20%5Capprox%200.22"> — about 22% of what perfect log-circuit readout would give. This is true across models that differ by orders of magnitude in their LM-head log-axis alignment. So the behavioural number isn’t a function of LM-head geometry; it’s a function of how loud the output-side frequency prior is relative to whatever the log circuit contributes.</p>
<p>A fresh probe trained on <code>=</code>-position residuals to predict <img src="https://latex.codecogs.com/png.latex?%5Clog_2(a%20%5Ccdot%20b)"> achieves <img src="https://latex.codecogs.com/png.latex?R%5E2%20=%200.989"> on held-out pairs. The model <em>has</em> computed something linearly equivalent to the answer’s log somewhere in its residual stream. But <strong>patching that direction barely moves the LM head’s argmax</strong> (slope 0.07 of perfect readout). So the direction is a downstream encoding, not the one the head actually reads. What direction the head <em>does</em> read for numeric output, and how it relates to the operand-side log axis, is the most interesting open question this work leaves.</p>
<p>The framing-level lesson is that small language models can have mechanistically rich circuits whose behavioural effect is suppressed by the output distribution they were trained against. “Can’t do multiplication” is consistent with “has parts of the multiplication circuit”, as long as a louder competing signal is shouting “use a common number” and the probe-found directions aren’t the ones the LM head listens to.</p>
</section>
<section id="caveats" class="level2">
<h2 class="anchored" data-anchor-id="caveats">Caveats</h2>
<p>GPT-2 ties its LM head to the embedding. Much of the apparent LM head ↔︎ log axis alignment is architectural. The Pythia-160M comparison addresses this for one untied model, but a wider scan across model families would be more convincing.</p>
<p>The causal-patching shifts are measured on relative logits among candidate products, not on argmax. Both small models get <img src="https://latex.codecogs.com/png.latex?0/200"> multiplications right. A model that can actually multiply (Pythia-1.4B, Llama-3.2-1B, Qwen-2.5-0.5B) would let the patch flip the argmax cleanly, not just shift relative logits.</p>
<p>The fitted answer-side probe at the <code>=</code> position has <img src="https://latex.codecogs.com/png.latex?R%5E2%20=%200.989"> on <img src="https://latex.codecogs.com/png.latex?(a,%20b)%20%5Cin%20%5B2,%2064%5D%5E2"> pairs. Whether the same direction carries <img src="https://latex.codecogs.com/png.latex?%5Clog_2(a%20%5Ccdot%20b)"> for much larger operands is untested.</p>
<p>The middle layers — where the operand-axis log gets translated to the answer-axis log — are a black box in this study. Path-patching across (layer, position) pairs would identify the attention heads that move log information from operand positions to the <code>=</code> position. The infrastructure for it is already in the patching code; I just didn’t run it.</p>
</section>
<section id="five-things-to-test-next" class="level2">
<h2 class="anchored" data-anchor-id="five-things-to-test-next">Five things to test next</h2>
<p><strong>Find the direction the LM head actually reads.</strong> The biggest open question raised by the answer-side causal test. The 0.989 probe is correlational; the LM head’s argmax is driven by some other direction. A sparse autoencoder or top-singular-vectors analysis of the LM-head columns for numeric tokens should expose what they actually decode.</p>
<p><strong>Exponentiation as a sharper test.</strong> If the model uses the log axis compositionally, patching <img src="https://latex.codecogs.com/png.latex?%5Clog(a)"> by <img src="https://latex.codecogs.com/png.latex?%5CDelta"> in an <code>a^b =</code> prompt should shift the output’s log by <img src="https://latex.codecogs.com/png.latex?b%20%5Ccdot%20%5CDelta"> — a quantitative prediction with one free parameter. Same patching code, different prompts.</p>
<p><strong>A bigger model that can actually multiply.</strong> Pythia-1.4B, Llama-3.2-1B, or Qwen-2.5-0.5B. If <img src="https://latex.codecogs.com/png.latex?%5Cbeta%20=%200.22"> is invariant across model scale, it’s a property of the training distribution, not capacity. If <img src="https://latex.codecogs.com/png.latex?%5Cbeta"> shoots up on a multiply-capable model, the limit at small scale really was “the head doesn’t act on the log axis hard enough”.</p>
<p><strong>Path-patch to find the translation layer.</strong> Where in the network does the operand-axis log get translated to whatever ends up at <code>=</code>? The patching infrastructure here is already 80% of what’s needed.</p>
<p><strong>Test the per-category axes.</strong> Within years, fit a year-trained log probe. Does it transfer cleanly to other years? Repeat for round numbers, number-words. If the integer-trained axis doesn’t transfer (the result above) but the within-category axes do transfer, the model has many private log axes, one per role.</p>
</section>
<section id="methodological-note" class="level2">
<h2 class="anchored" data-anchor-id="methodological-note">Methodological note</h2>
<p>If a probe trained at position A gives no signal at position B, that doesn’t mean the information isn’t in B — it might just mean B uses a different direction. Fit a fresh probe at the position you actually care about before concluding the circuit is absent. In this study, that one habit was the difference between “no circuit found” and “circuit found at <img src="https://latex.codecogs.com/png.latex?R%5E2%20=%200.989">”.</p>
<p>The shared-axis test worked the same way in reverse: an attractive hypothesis (“there’s one universal magnitude axis for all numeric tokens”) was falsified with about twenty minutes of compute and one transfer measurement. The Pythia comparison was the same shape — running the same probe on a second model with untied weights corrected a number I would otherwise have over-interpreted. Cheap probes are how you both rescue and kill ideas.</p>
</section>
<section id="code" class="level2">
<h2 class="anchored" data-anchor-id="code">Code</h2>
<table class="caption-top table">
<colgroup>
<col style="width: 37%">
<col style="width: 62%">
</colgroup>
<thead>
<tr class="header">
<th>File</th>
<th>Produces</th>
</tr>
</thead>
<tbody>
<tr class="odd">
<td><code>reasoning_router.py</code></td>
<td>Figure&nbsp;2</td>
</tr>
<tr class="even">
<td><code>probe_gpt2_log.py</code>, <code>probe_gpt2_controls.py</code></td>
<td>Figure&nbsp;3</td>
</tr>
<tr class="odd">
<td><code>probe_gpt2_shared_axis.py</code></td>
<td>Figure&nbsp;4</td>
</tr>
<tr class="even">
<td><code>probe_gpt2_patching.py</code></td>
<td>Figure&nbsp;5</td>
</tr>
<tr class="odd">
<td><code>probe_gpt2_lmhead.py</code></td>
<td>Figure&nbsp;8</td>
</tr>
<tr class="even">
<td><code>probe_gpt2_pipeline.py</code></td>
<td>Figure&nbsp;6</td>
</tr>
<tr class="odd">
<td><code>probe_gpt2_answer_side_causal.py</code></td>
<td>Figure&nbsp;7</td>
</tr>
<tr class="even">
<td><code>probe_gpt2_near_miss.py</code></td>
<td>Figure&nbsp;9</td>
</tr>
<tr class="odd">
<td><code>probe_pythia_compare.py</code>, <code>probe_pythia70_compare.py</code></td>
<td>data for Figure&nbsp;1</td>
</tr>
<tr class="even">
<td><code>make_summary_figure_v2.py</code></td>
<td>Figure&nbsp;1</td>
</tr>
</tbody>
</table>
<p>Dependencies: <code>torch</code>, <code>transformers</code>, <code>scikit-learn</code>, <code>matplotlib</code>. The full pipeline reproduces in about 20 minutes on a laptop CPU. GPT-2 small downloads at <img src="https://latex.codecogs.com/png.latex?%5Csim%20500"> MB; Pythia-160M at <img src="https://latex.codecogs.com/png.latex?%5Csim%20320"> MB.</p>


<!-- -->

</section>

 ]]></description>
  <category>interpretability</category>
  <category>mech-interp</category>
  <category>language models</category>
  <category>arithmetic</category>
  <guid>https://hli1234.github.io/probing-small-lms/posts/log_shortcut_findings/</guid>
  <pubDate>Sun, 15 Oct 2023 07:00:00 GMT</pubDate>
</item>
</channel>
</rss>
